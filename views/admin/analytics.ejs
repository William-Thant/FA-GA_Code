<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Sales Analytics - Isaac's Cardealership</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.0/dist/chart.umd.min.js"></script>
  <link rel="stylesheet" href="/theme.css">
  <style>
    body {
      background: linear-gradient(135deg, #0a0a0a 0%, #1a1a1a 100%);
      min-height: 100vh;
      color: #ffffff;
    }
    .page-header {
      background: linear-gradient(135deg, #000000 0%, #ff3333 50%, #000000 100%);
      color: white;
      padding: 50px 0;
      margin-bottom: 40px;
      border-bottom: 4px solid #ffd700;
      box-shadow: 0 15px 50px rgba(255, 51, 51, 0.4);
      position: relative;
    }
    .page-header::after {
      content: '⚡⚡⚡';
      position: absolute;
      right: 50px;
      top: 50%;
      transform: translateY(-50%);
      font-size: 3rem;
      opacity: 0.2;
    }
    .page-header h1 {
      font-weight: 900;
      text-transform: uppercase;
      letter-spacing: 4px;
      text-shadow: 0 0 30px rgba(255, 215, 0, 0.8);
    }
    .stat-card {
      background: linear-gradient(135deg, #1a1a1a 0%, #0a0a0a 100%);
      border-radius: 15px;
      padding: 25px;
      box-shadow: 0 10px 30px rgba(0,0,0,0.6);
      transition: all 0.3s ease;
      height: 100%;
      border: 2px solid #333;
      position: relative;
      overflow: hidden;
    }
    .stat-card::before {
      content: '';
      position: absolute;
      top: -50%;
      right: -50%;
      width: 200%;
      height: 200%;
      background: radial-gradient(circle, rgba(255,51,51,0.1) 0%, transparent 70%);
      transition: all 0.5s;
    }
    .stat-card:hover {
      transform: translateY(-8px);
      border-color: #ff3333;
      box-shadow: 0 15px 40px rgba(255, 51, 51, 0.4);
    }
    .stat-card:hover::before {
      top: -30%;
      right: -30%;
    }
    .stat-icon {
      width: 70px;
      height: 70px;
      border-radius: 15px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 2rem;
      margin-bottom: 15px;
      position: relative;
      z-index: 1;
    }
    .stat-value {
      font-size: 2.5rem;
      font-weight: 900;
      margin: 10px 0;
      font-family: 'Courier New', monospace;
      position: relative;
      z-index: 1;
    }
    .stat-label {
      color: #a8a8a8;
      font-size: 0.9rem;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 1px;
      position: relative;
      z-index: 1;
    }
    .chart-container {
      background: linear-gradient(135deg, #1a1a1a 0%, #0a0a0a 100%);
      border: 2px solid #333;
      border-radius: 15px;
      padding: 30px;
      box-shadow: 0 10px 30px rgba(0,0,0,0.6);
      margin-bottom: 30px;
      position: relative;
      height: 450px;
    }
    .chart-container:hover {
      border-color: #ff3333;
    }
    .chart-title {
      font-size: 1.3rem;
      font-weight: 700;
      margin-bottom: 25px;
      color: #ff3333;
      text-transform: uppercase;
      letter-spacing: 2px;
      border-bottom: 2px solid #ff3333;
      padding-bottom: 10px;
    }
    .table-container {
      background: linear-gradient(135deg, #1a1a1a 0%, #0a0a0a 100%);
      border: 2px solid #333;
      border-radius: 15px;
      padding: 30px;
      box-shadow: 0 10px 30px rgba(0,0,0,0.6);
      margin-bottom: 30px;
    }
    .table-container:hover {
      border-color: #ffd700;
    }
    .table {
      color: #ffffff;
      margin-bottom: 0;
    }
    .table thead th {
      background: linear-gradient(135deg, #ff3333 0%, #cc0000 100%);
      color: white;
      border: none;
      text-transform: uppercase;
      letter-spacing: 1px;
      font-size: 0.85rem;
      font-weight: 700;
      padding: 15px;
    }
    .table tbody tr {
      border-bottom: 1px solid #333;
      transition: all 0.3s;
      background: #0a0a0a;
    }
    .table tbody tr:hover {
      background: rgba(255, 51, 51, 0.1);
    }
    .table tbody td {
      padding: 15px;
      vertical-align: middle;
      color: #ffffff;
      background: #0a0a0a;
    }
    .payment-badge {
      padding: 8px 15px;
      border-radius: 10px;
      font-size: 0.85rem;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }
    .filter-section {
      background: linear-gradient(135deg, #1a1a1a 0%, #0a0a0a 100%);
      border: 2px solid #ffd700;
      border-radius: 15px;
      padding: 25px;
      margin-bottom: 30px;
      box-shadow: 0 10px 30px rgba(255, 215, 0, 0.2);
    }
    .form-select, .form-control {
      background: #0a0a0a;
      border: 2px solid #333;
      color: #ffffff;
      border-radius: 10px;
      padding: 12px;
      font-weight: 600;
    }
    .form-select:focus, .form-control:focus {
      background: #0a0a0a;
      border-color: #ff3333;
      color: #ffffff;
      box-shadow: 0 0 15px rgba(255, 51, 51, 0.3);
    }
    .form-label {
      color: #ffd700;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 1px;
      font-size: 0.9rem;
    }
    .badge {
      font-weight: 700;
      padding: 8px 15px;
      border-radius: 8px;
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }
  </style>
</head>
<body>
  <%- include('../partials/navbar') %>

  <div class="page-header">
    <div class="container">
      <h1><i class="fas fa-chart-line me-3"></i>🏁 SALES ANALYTICS</h1>
      <p class="mb-0" style="color: #ffd700; letter-spacing: 2px; font-weight: 600;">Real-time Revenue & Performance Metrics</p>
    </div>
  </div>

  <div class="container pb-5">
    
    <!-- Filter Section -->
    <div class="filter-section">
      <form method="GET" action="/admin/analytics" class="row g-3 align-items-end">
        <div class="col-md-4">
          <label class="form-label"><i class="fas fa-calendar-days me-2"></i>Time Period</label>
          <select name="period" class="form-select" onchange="this.form.submit()">
            <option value="7" <%= period === '7' ? 'selected' : '' %>>Last 7 Days</option>
            <option value="30" <%= period === '30' ? 'selected' : '' %>>Last 30 Days</option>
            <option value="90" <%= period === '90' ? 'selected' : '' %>>Last 90 Days</option>
            <option value="365" <%= period === '365' ? 'selected' : '' %>>Last Year</option>
          </select>
        </div>
      </form>
    </div>

    <!-- Key Metrics -->
    <div class="row mb-4">
      <div class="col-lg-3 col-md-6 mb-4">
        <div class="stat-card">
          <div class="stat-icon" style="background: linear-gradient(135deg, #ffd700 0%, #ccaa00 100%); color: #0a0a0a;">
            <i class="fas fa-dollar-sign"></i>
          </div>
          <div class="stat-label">Total Revenue</div>
          <div class="stat-value" style="color: #ffd700;">$<%= stats.totalRevenue.toFixed(2) %></div>
          <small class="stat-label">Last <%= period %> days</small>
        </div>
      </div>
      
      <div class="col-lg-3 col-md-6 mb-4">
        <div class="stat-card">
          <div class="stat-icon" style="background: linear-gradient(135deg, #00d4ff 0%, #0099cc 100%); color: #0a0a0a;">
            <i class="fas fa-coins"></i>
          </div>
          <div class="stat-label">Admin Commission</div>
          <div class="stat-value" style="color: #00d4ff;">$<%= stats.adminCommission.toFixed(2) %></div>
          <small class="stat-label">Platform Earnings (10%)</small>
        </div>
      </div>
      
      <div class="col-lg-3 col-md-6 mb-4">
        <div class="stat-card">
          <div class="stat-icon" style="background: linear-gradient(135deg, #ff3333 0%, #cc0000 100%); color: white;">
            <i class="fas fa-car"></i>
          </div>
          <div class="stat-label">Vehicles Sold</div>
          <div class="stat-value" style="color: #ff3333;"><%= stats.totalOrders %></div>
          <small class="stat-label">Completed Sales</small>
        </div>
      </div>
      
      <div class="col-lg-3 col-md-6 mb-4">
        <div class="stat-card">
          <div class="stat-icon" style="background: linear-gradient(135deg, #a855f7 0%, #7c3aed 100%); color: white;">
            <i class="fas fa-credit-card"></i>
          </div>
          <div class="stat-label">Payment Methods</div>
          <div class="stat-value" style="color: #a855f7;"><%= paymentMethods.length %></div>
          <small class="stat-label">Active Methods</small>
        </div>
      </div>
    </div>

    <!-- Charts Row -->
    <div class="row mb-4">
      <div class="col-lg-12 mb-4">
        <div class="chart-container">
          <h5 class="chart-title"><i class="fas fa-credit-card me-2"></i>Payment Methods Distribution</h5>
          <canvas id="paymentMethodsChart"></canvas>
        </div>
      </div>
    </div>

    <!-- Tables Row -->
    <div class="row">
      <!-- Top Customers -->
      <div class="col-lg-6 mb-4">
        <div class="table-container">
          <h5 class="chart-title"><i class="fas fa-trophy me-2"></i>Top Customers by Spending</h5>
          <div class="table-responsive">
            <table class="table">
              <thead>
                <tr>
                  <th>Rank</th>
                  <th>Customer</th>
                  <th>Orders</th>
                  <th>Total Spent</th>
                </tr>
              </thead>
              <tbody>
                <% topCustomers.forEach((customer, index) => { %>
                <tr>
                  <td>
                    <span class="badge" style="background: linear-gradient(135deg, #ffd700 0%, #ccaa00 100%); color: #0a0a0a;">
                      #<%= index + 1 %>
                    </span>
                  </td>
                  <td>
                    <i class="fas fa-user me-2" style="color: #00d4ff;"></i>
                    <strong><%= customer.name %></strong>
                  </td>
                  <td><span class="badge bg-info"><%= customer.orderCount %></span></td>
                  <td><strong style="color: #ffd700;">$<%= customer.totalSpent.toFixed(2) %></strong></td>
                </tr>
                <% }); %>
                <% if (topCustomers.length === 0) { %>
                <tr>
                  <td colspan="4" class="text-center" style="color: #a8a8a8;">No customer data available</td>
                </tr>
                <% } %>
              </tbody>
            </table>
          </div>
        </div>
      </div>

      <!-- Popular Discount Codes -->
      <div class="col-lg-6 mb-4">
        <div class="table-container">
          <h5 class="chart-title"><i class="fas fa-tags me-2"></i>Popular Discount Codes</h5>
          <div class="table-responsive">
            <table class="table">
              <thead>
                <tr>
                  <th>Code</th>
                  <th>Type</th>
                  <th>Usage</th>
                </tr>
              </thead>
              <tbody>
                <% discountCodes.forEach((code) => { %>
                <tr>
                  <td>
                    <span class="badge" style="background: linear-gradient(135deg, #ff3333 0%, #cc0000 100%);">
                      <%= code.code %>
                    </span>
                  </td>
                  <td>
                    <% if (code.discountType === 'percentage') { %>
                      <span class="payment-badge" style="background: rgba(0, 212, 255, 0.2); color: #00d4ff; border: 1px solid #00d4ff;">
                        <%= code.discountValue %>% OFF
                      </span>
                    <% } else { %>
                      <span class="payment-badge" style="background: rgba(255, 215, 0, 0.2); color: #ffd700; border: 1px solid #ffd700;">
                        $<%= code.discountValue %> OFF
                      </span>
                    <% } %>
                  </td>
                  <td><span style="color: #a8a8a8;"><%= code.timesUsed %> uses</span></td>
                </tr>
                <% }); %>
                <% if (discountCodes.length === 0) { %>
                <tr>
                  <td colspan="3" class="text-center" style="color: #a8a8a8;">No discount codes used</td>
                </tr>
                <% } %>
              </tbody>
            </table>
          </div>
        </div>
      </div>
    </div>

    <!-- Payment Provider Breakdown -->
    <div class="table-container">
      <h5 class="chart-title"><i class="fas fa-wallet me-2"></i>Payment Provider Performance</h5>
      <div class="table-responsive">
        <table class="table">
          <thead>
            <tr>
              <th>Payment Method</th>
              <th>Transactions</th>
              <th>Total Amount</th>
              <th>Average Transaction</th>
            </tr>
          </thead>
          <tbody>
            <% paymentMethods.forEach((method) => { %>
            <tr>
              <td>
                <% if (method.provider === 'Wallet') { %>
                  <i class="fas fa-wallet me-2" style="color: #ff3333;"></i>
                <% } else if (method.provider === 'NETS') { %>
                  <i class="fas fa-qrcode me-2" style="color: #ffd700;"></i>
                <% } else if (method.provider === 'PayPal') { %>
                  <i class="fab fa-paypal me-2" style="color: #00d4ff;"></i>
                <% } else if (method.provider === 'Stripe') { %>
                  <i class="fab fa-stripe me-2" style="color: #a855f7;"></i>
                <% } else if (method.provider === 'Gift Card') { %>
                  <i class="fas fa-gift me-2" style="color: #ff3333;"></i>
                <% } else { %>
                  <i class="fas fa-credit-card me-2"></i>
                <% } %>
                <strong><%= method.provider %></strong>
              </td>
              <td><span class="badge bg-secondary"><%= method.count %></span></td>
              <td><strong style="color: #ffd700;">$<%= method.totalAmount.toFixed(2) %></strong></td>
              <td style="color: #00d4ff;">$<%= method.avgAmount.toFixed(2) %></td>
            </tr>
            <% }); %>
          </tbody>
        </table>
      </div>
    </div>

  </div>

  <script>
    // Payment Methods Pie Chart
    const paymentCtx = document.getElementById('paymentMethodsChart').getContext('2d');
    new Chart(paymentCtx, {
      type: 'doughnut',
      data: {
        labels: <%- JSON.stringify(paymentMethodData.labels) %>,
        datasets: [{
          data: <%- JSON.stringify(paymentMethodData.values) %>,
          backgroundColor: [
            'rgba(255, 51, 51, 0.8)',
            'rgba(255, 215, 0, 0.8)',
            'rgba(0, 212, 255, 0.8)',
            'rgba(168, 85, 247, 0.8)',
            'rgba(255, 99, 72, 0.8)'
          ],
          borderWidth: 3,
          borderColor: '#0a0a0a'
        }]
      },
      options: {
        responsive: true,
        maintainAspectRatio: false,
        plugins: {
          legend: {
            position: 'bottom',
            labels: {
              color: '#ffffff',
              font: {
                size: 14,
                weight: 'bold'
              },
              padding: 20
            }
          },
          tooltip: {
            callbacks: {
              label: function(context) {
                const total = context.dataset.data.reduce((a, b) => a + b, 0);
                const percentage = ((context.parsed / total) * 100).toFixed(1);
                return context.label + ': ' + context.parsed + ' (' + percentage + '%)';
              }
            },
            backgroundColor: 'rgba(0, 0, 0, 0.9)',
            titleColor: '#ffd700',
            bodyColor: '#ffffff',
            borderColor: '#ff3333',
            borderWidth: 2,
            padding: 15,
            displayColors: true,
            titleFont: {
              size: 16,
              weight: 'bold'
            },
            bodyFont: {
              size: 14
            }
          }
        }
      }
    });
  </script>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

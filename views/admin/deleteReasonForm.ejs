<!DOCTYPE html>
<html lang="en" data-bs-theme="dark">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
  <link rel="stylesheet" href="/theme.css">
  <title>Delete Vehicle - Isaac's Car Dealership</title>
  <style>
    body {
      background: linear-gradient(135deg, #0a0a0a 0%, #1a1a1a 100%);
      min-height: 100vh;
      color: #ffffff;
    }

    .delete-header {
      background: linear-gradient(135deg, #000000 0%, #1a1a1a 50%, #ff3333 100%);
      color: white;
      padding: 60px 0;
      margin-bottom: 40px;
      border-bottom: 3px solid #ff3333;
      box-shadow: 0 10px 40px rgba(255, 51, 51, 0.3);
    }

    .delete-header h1 {
      font-weight: 800;
      text-transform: uppercase;
      letter-spacing: 3px;
      font-size: 2.5rem;
      text-shadow: 0 0 20px rgba(255, 51, 51, 0.5);
    }

    .delete-card {
      background: linear-gradient(135deg, #1a1a1a 0%, #0a0a0a 100%);
      border: 2px solid #333;
      border-radius: 15px;
      box-shadow: 0 10px 30px rgba(0,0,0,0.5);
      padding: 30px;
      margin-bottom: 30px;
      transition: all 0.3s ease;
    }

    .delete-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 15px 50px rgba(255, 51, 51, 0.3);
      border-color: #ff3333;
    }

    .vehicle-info {
      background: rgba(255, 51, 51, 0.1);
      border-left: 4px solid #ff3333;
      padding: 20px;
      border-radius: 10px;
      margin-bottom: 20px;
    }

    .vehicle-info h5 {
      color: #ff3333;
      margin-bottom: 10px;
    }

    .vehicle-image {
      max-width: 150px;
      border-radius: 10px;
      margin-right: 20px;
      border: 2px solid #ff3333;
    }

    .form-control, .form-select {
      background: #2a2a2a;
      border: 2px solid #333;
      color: #ffffff;
      padding: 0.75rem;
      border-radius: 8px;
    }

    .form-control:focus, .form-select:focus {
      background: #2a2a2a;
      border-color: #ff3333;
      color: #ffffff;
      box-shadow: 0 0 0 0.2rem rgba(255, 51, 51, 0.25);
    }

    .form-label {
      color: #a8a8a8;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 1px;
      font-size: 0.85rem;
      margin-bottom: 0.5rem;
    }

    .btn-delete {
      background: linear-gradient(135deg, #dc3545 0%, #c82333 100%);
      border: none;
      color: white;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 1px;
      padding: 12px 30px;
      border-radius: 10px;
      transition: all 0.3s;
    }

    .btn-delete:hover {
      background: linear-gradient(135deg, #c82333 0%, #bd2130 100%);
      transform: scale(1.05);
      box-shadow: 0 8px 25px rgba(220, 53, 69, 0.5);
      color: white;
    }

    .btn-cancel {
      background: linear-gradient(135deg, #6c757d 0%, #495057 100%);
      border: none;
      color: white;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 1px;
      padding: 12px 30px;
      border-radius: 10px;
      transition: all 0.3s;
    }

    .btn-cancel:hover {
      background: linear-gradient(135deg, #495057 0%, #343a40 100%);
      transform: scale(1.05);
      box-shadow: 0 8px 25px rgba(108, 117, 125, 0.5);
      color: white;
    }

    .warning-alert {
      background: rgba(220, 53, 69, 0.2);
      border: 2px solid #dc3545;
      color: #ffffff;
      border-radius: 10px;
      padding: 15px;
      margin-bottom: 20px;
    }

    .warning-alert i {
      color: #ff3333;
      margin-right: 10px;
    }
  </style>
</head>
<body>
    <%- include('../partials/navbar') %>

    <div class="delete-header">
      <div class="container text-center">
        <h1><i class="fas fa-exclamation-triangle me-3"></i>Delete Vehicle</h1>
        <p>Confirm vehicle removal from inventory</p>
      </div>
    </div>

    <div class="container pb-5">
      <div class="row justify-content-center">
        <div class="col-lg-8">
          <div class="delete-card">
            <div class="warning-alert">
              <i class="fas fa-shield-alt"></i>
              <strong>Admin Action Required</strong> - Please provide a reason for removing this vehicle from inventory.
            </div>

            <!-- Vehicle Information -->
            <div class="vehicle-info">
              <h5><i class="fas fa-car me-2"></i>Vehicle Details</h5>
              <div class="row">
                <div class="col-md-4 text-center">
                  <% if (product.image) { %>
                    <img src="/images/<%= product.image %>" alt="<%= product.productName %>" class="vehicle-image">
                  <% } %>
                </div>
                <div class="col-md-8">
                  <p><strong>Name:</strong> <%= product.productName %></p>
                  <p><strong>Price:</strong> $<%= parseFloat(product.price).toFixed(2) %></p>
                  <p><strong>Stock:</strong> <%= product.quantity %> units</p>
                  <p><strong>Category:</strong> <%= product.category || 'N/A' %></p>
                </div>
              </div>
            </div>

            <!-- Delete Reason Form -->
            <form method="POST" action="/deleteProduct/<%= product.id %>/confirm">
              <div class="mb-4">
                <label for="reason" class="form-label"><i class="fas fa-comment me-2"></i>Reason for Deletion</label>
                <select class="form-select" id="reason" name="reason" required>
                  <option value="">-- Select a Reason --</option>
                  <option value="duplicate">Duplicate Listing</option>
                  <option value="defective">Vehicle Defective/Damaged</option>
                  <option value="incomplete">Incomplete Information</option>
                  <option value="sold">Already Sold</option>
                  <option value="pricing">Incorrect Pricing Information</option>
                  <option value="other">Other</option>
                </select>
              </div>

              <div class="mb-4">
                <label for="details" class="form-label"><i class="fas fa-list me-2"></i>Additional Details (Optional)</label>
                <textarea class="form-control" id="details" name="details" rows="4" placeholder="Provide any additional details about why this vehicle is being removed..."></textarea>
              </div>

              <div class="d-flex gap-3">
                <button type="submit" class="btn btn-delete">
                  <i class="fas fa-trash me-2"></i>Confirm Deletion
                </button>
                <a href="/inventory" class="btn btn-cancel">
                  <i class="fas fa-times me-2"></i>Cancel
                </a>
              </div>
            </form>
          </div>
        </div>
      </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

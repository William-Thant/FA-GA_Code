<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Discount Code Management - Isaac's Cardealership</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
  <style>
    body {
      background: linear-gradient(135deg, #0a0a0a 0%, #1a1a1a 100%);
      min-height: 100vh;
      color: #ffffff;
    }
    .page-header {
      background: linear-gradient(135deg, #ffd700 0%, #000000 50%, #ffd700 100%);
      color: white;
      padding: 50px 0;
      margin-bottom: 40px;
      border-bottom: 4px solid #ffd700;
      box-shadow: 0 15px 50px rgba(255, 215, 0, 0.4);
    }
    .page-header h1 {
      font-weight: 900;
      text-transform: uppercase;
      letter-spacing: 4px;
      text-shadow: 0 0 30px rgba(255, 51, 51, 0.8);
    }
    .form-card {
      background: linear-gradient(135deg, #1a1a1a 0%, #0a0a0a 100%);
      border: 2px solid #ffd700;
      border-radius: 15px;
      padding: 30px;
      margin-bottom: 30px;
      box-shadow: 0 10px 30px rgba(255, 215, 0, 0.3);
    }
    .form-card h3 {
      color: #ffd700;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 2px;
      border-bottom: 2px solid #ffd700;
      padding-bottom: 10px;
      margin-bottom: 20px;
    }
    .form-label {
      color: #ffd700;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 1px;
      font-size: 0.9rem;
    }
    .form-control, .form-select {
      background: #0a0a0a;
      border: 2px solid #333;
      color: #ffffff;
      border-radius: 10px;
      padding: 12px;
      font-weight: 600;
    }
    .form-control:focus, .form-select:focus {
      background: #0a0a0a;
      border-color: #ffd700;
      color: #ffffff;
      box-shadow: 0 0 15px rgba(255, 215, 0, 0.3);
    }
    .form-control::placeholder, .form-select::placeholder {
      color: #ffffff !important;
      opacity: 0.8;
    }
    .text-muted {
      color: #ffffff !important;
      opacity: 0.7;
      font-size: 0.85rem;
    }
    input[type="datetime-local"]::-webkit-calendar-picker-indicator {
      filter: invert(1);
      cursor: pointer;
      opacity: 1;
    }
    .btn-create {
      background: linear-gradient(135deg, #ffd700 0%, #ccaa00 100%);
      border: none;
      color: #0a0a0a;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 1px;
      padding: 15px 40px;
      border-radius: 10px;
      transition: all 0.3s;
    }
    .btn-create:hover {
      background: linear-gradient(135deg, #ccaa00 0%, #ffd700 100%);
      transform: scale(1.05);
      box-shadow: 0 8px 25px rgba(255, 215, 0, 0.5);
      color: #0a0a0a;
    }
    .codes-list {
      background: linear-gradient(135deg, #1a1a1a 0%, #0a0a0a 100%);
      border: 2px solid #333;
      border-radius: 15px;
      padding: 30px;
      box-shadow: 0 10px 30px rgba(0,0,0,0.6);
    }
    .codes-list h3 {
      color: #ff3333;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 2px;
      border-bottom: 2px solid #ff3333;
      padding-bottom: 10px;
      margin-bottom: 20px;
    }
    .table {
      color: #ffffff;
    }
    .table thead th {
      background: linear-gradient(135deg, #ff3333 0%, #cc0000 100%);
      color: white;
      border: none;
      text-transform: uppercase;
      letter-spacing: 1px;
      font-size: 0.85rem;
      font-weight: 700;
      padding: 15px;
    }
    .table tbody tr {
      border-bottom: 1px solid #333;
      background: #0a0a0a;
    }
    .table tbody tr:hover {
      background: rgba(255, 215, 0, 0.1);
    }
    .table tbody td {
      padding: 15px;
      color: #ffffff;
      background: #0a0a0a;
    }
    .badge {
      font-weight: 700;
      padding: 8px 15px;
      border-radius: 8px;
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }
    .alert {
      border-radius: 15px;
      border: 2px solid;
      font-weight: 600;
    }
    .alert-danger {
      background: rgba(255, 51, 51, 0.2);
      border-color: #ff3333;
      color: #ffffff;
    }
    .alert-success {
      background: rgba(255, 215, 0, 0.2);
      border-color: #ffd700;
      color: #ffffff;
    }
    .btn-close {
      filter: invert(1);
    }
    .btn-danger {
      background: linear-gradient(135deg, #ff3333 0%, #cc0000 100%);
      border: none;
      color: white;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 1px;
    }
    .btn-danger:hover {
      background: linear-gradient(135deg, #cc0000 0%, #ff3333 100%);
      box-shadow: 0 8px 25px rgba(255, 51, 51, 0.5);
      color: white;
    }
  </style>
</head>
<body>
<%- include('../partials/navbar') %>

<div class="page-header">
  <div class="container">
    <h1><i class="fas fa-tags"></i> 💰 DISCOUNT CODE MANAGEMENT</h1>
    <p class="mb-0" style="color: #ffffff; letter-spacing: 2px; font-weight: 600;">Create & Manage Promotional Codes</p>
  </div>
</div>

<div class="container pb-5">
  <!-- Flash Messages -->
  <% if (typeof messages !== 'undefined' && messages) { %>
    <% if (messages.error && messages.error.length) { %>
      <div class="alert alert-danger alert-dismissible fade show" role="alert">
        <i class="fas fa-exclamation-circle"></i> <%= messages.error.join ? messages.error.join(', ') : messages.error %>
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
      </div>
    <% } %>
    <% if (messages.success && messages.success.length) { %>
      <div class="alert alert-success alert-dismissible fade show" role="alert">
        <i class="fas fa-check-circle"></i> <%= messages.success.join ? messages.success.join(', ') : messages.success %>
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
      </div>
    <% } %>
  <% } %>

  <!-- Create New Discount Code Form -->
  <div class="form-card">
    <h3><i class="fas fa-plus-circle"></i> Create New Discount Code</h3>
    <form action="/admin/discount-codes/create" method="POST">
      <div class="row">
        <div class="col-md-6 mb-3">
          <label class="form-label"><i class="fas fa-tag me-2"></i>Code</label>
          <input type="text" class="form-control" name="code" required 
                 placeholder="e.g., SUMMER25" 
                 pattern="[A-Z0-9]+" 
                 title="Use only uppercase letters and numbers"
                 style="text-transform: uppercase;">
          <small class="text-muted">Only uppercase letters and numbers</small>
        </div>
        
        <div class="col-md-6 mb-3">
          <label class="form-label"><i class="fas fa-percentage me-2"></i>Discount Type</label>
          <select class="form-select" name="type" id="discountType" required>
            <option value="percentage">Percentage Off</option>
            <option value="fixed">Fixed Amount Off</option>
          </select>
        </div>

        <div class="col-md-6 mb-3">
          <label class="form-label"><i class="fas fa-dollar-sign me-2"></i>Value</label>
          <input type="number" class="form-control" name="value" required 
                 min="0.01" step="0.01" placeholder="e.g., 10.00">
          <small class="text-muted" id="valueHint">Percentage (e.g., 10 for 10%)</small>
        </div>

        <div class="col-md-6 mb-3">
          <label class="form-label"><i class="fas fa-shopping-cart me-2"></i>Minimum Purchase ($)</label>
          <input type="number" class="form-control" name="minPurchase" 
                 min="0" step="0.01" value="0" placeholder="0.00">
          <small class="text-muted">Optional - leave 0 for no minimum</small>
        </div>

        <div class="col-md-6 mb-3">
          <label class="form-label"><i class="fas fa-repeat me-2"></i>Maximum Uses</label>
          <input type="number" class="form-control" name="maxUses" 
                 min="1" placeholder="Unlimited">
          <small class="text-muted">Leave empty for unlimited uses</small>
        </div>

        <div class="col-md-6 mb-3">
          <label class="form-label"><i class="fas fa-calendar me-2"></i>Expiry Date</label>
          <input type="datetime-local" class="form-control" name="expiryDate">
          <small class="text-muted">Leave empty for no expiration</small>
        </div>
      </div>

      <div class="form-check mb-3">
        <input class="form-check-input" type="checkbox" name="active" id="activeCheck" checked>
        <label class="form-check-label" for="activeCheck" style="color: #ffffff; text-transform: none;">
          Active (code can be used immediately)
        </label>
      </div>

      <button type="submit" class="btn btn-create w-100">
        <i class="fas fa-plus-circle"></i> Create Discount Code
      </button>
    </form>
  </div>

  <!-- Existing Discount Codes -->
  <div class="codes-list">
    <h3><i class="fas fa-list"></i> Existing Discount Codes</h3>
    <% if (discountCodes.length === 0) { %>
      <p class="text-center" style="color: #a8a8a8; padding: 30px 0;">No discount codes created yet</p>
    <% } else { %>
      <div class="table-responsive">
        <table class="table">
          <thead>
            <tr>
              <th>Code</th>
              <th>Type</th>
              <th>Value</th>
              <th>Min Purchase</th>
              <th>Uses</th>
              <th>Expiry</th>
              <th>Status</th>
              <th>Action</th>
            </tr>
          </thead>
          <tbody>
            <% discountCodes.forEach(code => { %>
              <tr>
                <td><strong style="color: #ffd700;"><%= code.code %></strong></td>
                <td>
                  <span class="badge <%= code.type === 'percentage' ? 'bg-info' : 'bg-warning' %>">
                    <%= code.type === 'percentage' ? 'Percentage' : 'Fixed' %>
                  </span>
                </td>
                <td style="color: #00d4ff;">
                  <%= code.type === 'percentage' ? code.value + '%' : '$' + parseFloat(code.value).toFixed(2) %>
                </td>
                <td style="color: #a8a8a8;">$<%= parseFloat(code.minPurchase).toFixed(2) %></td>
                <td>
                  <span style="color: #ffffff;"><%= code.usedCount || 0 %></span>
                  <% if (code.maxUses) { %>
                    <span style="color: #a8a8a8;"> / <%= code.maxUses %></span>
                  <% } else { %>
                    <span style="color: #a8a8a8;"> / ∞</span>
                  <% } %>
                </td>
                <td style="color: #a8a8a8;">
                  <% if (code.expiryDate) { %>
                    <%= new Date(code.expiryDate).toLocaleDateString() %>
                  <% } else { %>
                    Never
                  <% } %>
                </td>
                <td>
                  <% if (code.active) { %>
                    <span class="badge bg-success">Active</span>
                  <% } else { %>
                    <span class="badge bg-secondary">Inactive</span>
                  <% } %>
                </td>
                <td>
                  <form action="/admin/discount-codes/delete" method="POST" style="display: inline;" 
                        onsubmit="return confirm('Are you sure you want to delete this discount code?');">
                    <input type="hidden" name="codeId" value="<%= code.id %>">
                    <button type="submit" class="btn btn-danger btn-sm">
                      <i class="fas fa-trash"></i>
                    </button>
                  </form>
                </td>
              </tr>
            <% }); %>
          </tbody>
        </table>
      </div>
    <% } %>
  </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
<script>
  // Update value hint based on discount type
  document.getElementById('discountType').addEventListener('change', function() {
    const hint = document.getElementById('valueHint');
    if (this.value === 'percentage') {
      hint.textContent = 'Percentage (e.g., 10 for 10% off)';
    } else {
      hint.textContent = 'Fixed amount (e.g., 50.00 for $50 off)';
    }
  });

  // Force uppercase on code input
  const codeInput = document.querySelector('input[name="code"]');
  codeInput.addEventListener('input', function() {
    this.value = this.value.toUpperCase();
  });
</script>
</body>
</html>

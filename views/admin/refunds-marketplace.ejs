<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Refund Requests - Admin Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        body {
            background: linear-gradient(135deg, #0a0a0a 0%, #1a1a1a 100%);
            min-height: 100vh;
            color: #ffffff !important;
        }
        .page-header {
            background: linear-gradient(135deg, #ff3333 0%, #cc0000 100%);
            padding: 30px;
            border-radius: 15px;
            margin-bottom: 30px;
            box-shadow: 0 10px 30px rgba(255, 51, 51, 0.3);
        }
        .page-header h1 {
            margin: 0;
            color: #ffffff !important;
        }
        .stats-card {
            background: linear-gradient(135deg, #1a1a1a 0%, #2a2a2a 100%);
            border: 2px solid #333;
            border-radius: 15px;
            padding: 25px;
            margin-bottom: 20px;
            text-align: center;
        }
        .stats-card h5 {
            font-size: 2.5rem;
            color: #ff3333 !important;
            margin-bottom: 10px;
        }
        .stats-card p {
            color: #ccc !important;
            margin: 0;
        }
        .refund-card {
            background: linear-gradient(135deg, #1a1a1a 0%, #2a2a2a 100%);
            border: 2px solid #333;
            border-radius: 15px;
            padding: 25px;
            margin-bottom: 20px;
            transition: all 0.3s ease;
        }
        .refund-card:hover {
            border-color: #ff3333;
            transform: translateY(-5px);
            box-shadow: 0 10px 30px rgba(255, 51, 51, 0.2);
        }
        .refund-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
            padding-bottom: 15px;
            border-bottom: 2px solid #333;
        }
        .refund-id {
            font-size: 1.2rem;
            font-weight: bold;
            color: #ff3333 !important;
        }
        .status-badge {
            padding: 8px 20px;
            border-radius: 25px;
            font-weight: bold;
            font-size: 0.85rem;
        }
        .status-pending {
            background: linear-gradient(135deg, #ffc107 0%, #e0a800 100%);
            color: #000000 !important;
        }
        .status-approved {
            background: linear-gradient(135deg, #28a745 0%, #1e7e34 100%);
            color: #ffffff !important;
        }
        .status-rejected {
            background: linear-gradient(135deg, #dc3545 0%, #bd2130 100%);
            color: #ffffff !important;
        }
        .refund-details {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 20px;
            margin-bottom: 20px;
        }
        .detail-item {
            display: flex;
            flex-direction: column;
        }
        .detail-label {
            color: #999 !important;
            font-size: 0.85rem;
            text-transform: uppercase;
            margin-bottom: 5px;
        }
        .detail-value {
            color: #ffffff !important;
            font-weight: bold;
        }
        .customer-info {
            background: rgba(0, 0, 0, 0.3);
            border-left: 4px solid #ff3333;
            padding: 15px;
            border-radius: 5px;
        }
        .no-refunds {
            text-align: center;
            padding: 60px 20px;
            background: linear-gradient(135deg, #1a1a1a 0%, #2a2a2a 100%);
            border-radius: 15px;
            color: #999 !important;
        }
        .no-refunds i {
            font-size: 4rem;
            color: #ff3333;
            margin-bottom: 20px;
        }
    </style>
</head>
<body>
    <%- include('../partials/navbar') %>
    
    <div class="container" style="margin-top: 30px; margin-bottom: 50px;">
        <div class="page-header">
            <h1><i class="fas fa-undo"></i> Marketplace Refund Requests</h1>
        </div>
        
        <% if (messages.success && messages.success.length > 0) { %>
            <div class="alert alert-success">
                <%= messages.success[0] %>
            </div>
        <% } %>
        
        <% if (messages.error && messages.error.length > 0) { %>
            <div class="alert alert-danger">
                <%= messages.error[0] %>
            </div>
        <% } %>
        
        <!-- Stats Overview -->
        <div class="row mb-4">
            <div class="col-md-3">
                <div class="stats-card">
                    <h5><%= refunds.length %></h5>
                    <p>Total Requests</p>
                </div>
            </div>
            <div class="col-md-3">
                <div class="stats-card">
                    <h5><%= refunds.filter(r => r.status === 'pending').length %></h5>
                    <p>Pending</p>
                </div>
            </div>
            <div class="col-md-3">
                <div class="stats-card">
                    <h5>$<%= stats.totalCommission ? stats.totalCommission.toFixed(2) : '0.00' %></h5>
                    <p>Admin Commission</p>
                </div>
            </div>
            <div class="col-md-3">
                <div class="stats-card">
                    <h5>$<%= stats.totalSales ? stats.totalSales.toFixed(2) : '0.00' %></h5>
                    <p>Total Sales</p>
                </div>
            </div>
        </div>
        
        <% if (refunds.length === 0) { %>
            <div class="no-refunds">
                <i class="fas fa-inbox"></i>
                <h3 style="color: #ffffff !important;">No Refund Requests</h3>
                <p>There are no refund requests to review at this time.</p>
            </div>
        <% } else { %>
            <% refunds.forEach(refund => { %>
                <div class="refund-card">
                    <div class="refund-header">
                        <div>
                            <div class="refund-id">Request #<%= refund.id %></div>
                            <div style="color: #999; font-size: 0.85rem;">
                                <i class="fas fa-calendar"></i>
                                <%= new Date(refund.requested_at).toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' }) %>
                            </div>
                        </div>
                        <span class="status-badge status-<%= refund.status %>">
                            <i class="fas fa-<%= refund.status === 'pending' ? 'clock' : refund.status === 'approved' ? 'check' : 'times' %>"></i>
                            <%= refund.status.toUpperCase() %>
                        </span>
                    </div>
                    
                    <div class="customer-info mb-4">
                        <strong style="color: #ff3333;">Customer: </strong><%= refund.customerName %> (<%= refund.customerEmail %>)<br>
                        <strong style="color: #ff3333;">Order: </strong>#<%= refund.orderId %> - 
                        Placed <%= new Date(refund.orderDate).toLocaleDateString('en-US', { year: 'numeric', month: 'short', day: 'numeric' }) %>
                    </div>
                    
                    <div class="refund-details">
                        <div class="detail-item">
                            <span class="detail-label"><i class="fas fa-car"></i> Product</span>
                            <span class="detail-value"><%= refund.productName %></span>
                        </div>
                        <div class="detail-item">
                            <span class="detail-label"><i class="fas fa-user-tie"></i> Seller</span>
                            <span class="detail-value"><%= refund.sellerName %></span>
                        </div>
                        <div class="detail-item">
                            <span class="detail-label"><i class="fas fa-dollar-sign"></i> Refund Amount</span>
                            <span class="detail-value" style="color: #ff3333 !important;">$<%= refund.amount.toFixed(2) %></span>
                        </div>
                        <div class="detail-item">
                            <span class="detail-label"><i class="fas fa-percentage"></i> Admin Commission (10%)</span>
                            <span class="detail-value" style="color: #ffc107 !important;">$<%= refund.admin_commission.toFixed(2) %></span>
                        </div>
                    </div>
                    
                    <div class="detail-item mb-3">
                        <span class="detail-label"><i class="fas fa-align-left"></i> Reason</span>
                        <span class="detail-value" style="color: #ccc !important; line-height: 1.6;">
                            <%= refund.reason %>
                        </span>
                    </div>
                    
                    <% if (refund.restore_inventory) { %>
                        <div style="background: rgba(40, 167, 69, 0.1); border-left: 4px solid #28a745; padding: 10px; border-radius: 5px; margin-bottom: 15px;">
                            <i class="fas fa-check" style="color: #28a745;"></i>
                            <span style="color: #28a745 !important; margin-left: 10px;">Inventory restored</span>
                        </div>
                    <% } %>
                    
                    <% if (refund.status === 'pending') { %>
                        <div class="row mt-3">
                            <div class="col-md-6">
                                <a href="/admin/refunds/<%= refund.id %>/approve" class="btn btn-success w-100">
                                    <i class="fas fa-check"></i> Approve
                                </a>
                            </div>
                            <div class="col-md-6">
                                <button class="btn btn-danger w-100" onclick="rejectRefund(<%= refund.id %>)">
                                    <i class="fas fa-times"></i> Reject
                                </button>
                            </div>
                        </div>
                    <% } else if (refund.processed_by && refund.processed_at) { %>
                        <div style="background: rgba(0, 0, 0, 0.3); padding: 10px; border-radius: 5px; font-size: 0.85rem;">
                            <span style="color: #999;">Processed on <%= new Date(refund.processed_at).toLocaleDateString() %></span>
                        </div>
                    <% } %>
                </div>
            <% }); %>
        <% } %>
    </div>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

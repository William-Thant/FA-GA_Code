<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Refund Management - Isaac's Cardealership</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
  <style>
    body {
      background: linear-gradient(135deg, #0a0a0a 0%, #1a1a1a 100%);
      min-height: 100vh;
      color: #ffffff;
    }
    .page-header {
      background: linear-gradient(135deg, #ff3333 0%, #000000 50%, #ffd700 100%);
      color: white;
      padding: 50px 0;
      margin-bottom: 40px;
      border-bottom: 4px solid #ff3333;
      box-shadow: 0 15px 50px rgba(255, 51, 51, 0.4);
    }
    .page-header h1 {
      font-weight: 900;
      text-transform: uppercase;
      letter-spacing: 4px;
      text-shadow: 0 0 30px rgba(255, 215, 0, 0.8);
    }
    .stats-overview {
      background: linear-gradient(135deg, #1a1a1a 0%, #0a0a0a 100%);
      border: 2px solid #ffd700;
      border-radius: 15px;
      padding: 25px;
      margin-bottom: 30px;
      box-shadow: 0 10px 30px rgba(255, 215, 0, 0.2);
    }
    .stats-overview .row > div {
      border-right: 2px solid #333;
    }
    .stats-overview .row > div:last-child {
      border-right: none;
    }
    .stats-overview h5 {
      font-size: 2rem;
      font-weight: 900;
      color: #ffd700;
      font-family: 'Courier New', monospace;
    }
    .stats-overview small {
      color: #a8a8a8;
      text-transform: uppercase;
      letter-spacing: 1px;
      font-weight: 700;
      font-size: 0.85rem;
    }
    .order-card {
      background: linear-gradient(135deg, #1a1a1a 0%, #0a0a0a 100%);
      border-left: 5px solid #ff3333;
      border-radius: 15px;
      padding: 25px;
      margin-bottom: 25px;
      box-shadow: 0 10px 30px rgba(0,0,0,0.6);
      transition: all 0.3s;
      position: relative;
      overflow: hidden;
    }
    .order-card::before {
      content: '';
      position: absolute;
      top: 0;
      right: 0;
      width: 150px;
      height: 100%;
      background: linear-gradient(90deg, transparent, rgba(255, 51, 51, 0.05));
    }
    .order-card:hover {
      transform: translateX(5px);
      border-left-color: #ffd700;
      box-shadow: 0 15px 40px rgba(255, 215, 0, 0.3);
    }
    .order-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      border-bottom: 2px solid #333;
      padding-bottom: 15px;
      margin-bottom: 20px;
    }
    .order-header h5 {
      color: #ff3333;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 2px;
    }
    .badge-refund-none {
      background: linear-gradient(135deg, #00d4ff 0%, #0099cc 100%);
      color: #0a0a0a;
      font-weight: 700;
      padding: 10px 20px;
      border-radius: 10px;
      text-transform: uppercase;
      letter-spacing: 1px;
    }
    .badge-refund-partial {
      background: linear-gradient(135deg, #ffd700 0%, #ccaa00 100%);
      color: #0a0a0a;
      font-weight: 700;
      padding: 10px 20px;
      border-radius: 10px;
      text-transform: uppercase;
      letter-spacing: 1px;
    }
    .badge-refund-full {
      background: linear-gradient(135deg, #ff3333 0%, #cc0000 100%);
      color: white;
      font-weight: 700;
      padding: 10px 20px;
      border-radius: 10px;
      text-transform: uppercase;
      letter-spacing: 1px;
    }
    .order-card h6 {
      color: #ffd700;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 1px;
      font-size: 0.9rem;
      margin-bottom: 10px;
    }
    .order-card hr {
      border-color: #333;
      margin: 20px 0;
    }
    .refund-actions button {
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 1px;
      padding: 12px 25px;
      border-radius: 10px;
      transition: all 0.3s;
    }
    .btn-warning {
      background: linear-gradient(135deg, #ffd700 0%, #ccaa00 100%);
      border: none;
      color: #0a0a0a;
    }
    .btn-warning:hover {
      background: linear-gradient(135deg, #ccaa00 0%, #ffd700 100%);
      transform: scale(1.05);
      box-shadow: 0 8px 25px rgba(255, 215, 0, 0.5);
      color: #0a0a0a;
    }
    .modal-content {
      background: linear-gradient(135deg, #1a1a1a 0%, #0a0a0a 100%);
      border: 2px solid #ff3333;
      color: #ffffff;
    }
    .modal-header {
      border-bottom: 2px solid #ff3333;
    }
    .modal-title {
      color: #ffd700;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 2px;
    }
    .modal-footer {
      border-top: 2px solid #333;
    }
    .form-label {
      color: #ffd700;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 1px;
      font-size: 0.9rem;
    }
    .form-control, .form-select {
      background: #0a0a0a;
      border: 2px solid #333;
      color: #ffffff;
      border-radius: 10px;
      padding: 12px;
    }
    .form-control:focus, .form-select:focus {
      background: #0a0a0a;
      border-color: #ff3333;
      color: #ffffff;
      box-shadow: 0 0 15px rgba(255, 51, 51, 0.3);
    }
    .alert {
      border-radius: 15px;
      border: 2px solid;
      font-weight: 600;
    }
    .alert-danger {
      background: rgba(255, 51, 51, 0.2);
      border-color: #ff3333;
      color: #ffffff;
    }
    .alert-success {
      background: rgba(0, 212, 255, 0.2);
      border-color: #00d4ff;
      color: #ffffff;
    }
    .alert-info {
      background: rgba(255, 215, 0, 0.2);
      border-color: #ffd700;
      color: #ffffff;
    }
    .btn-close {
      filter: invert(1);
    }
    .btn-secondary {
      background: #333;
      border: none;
      color: #ffffff;
    }
    .btn-secondary:hover {
      background: #555;
      color: #ffffff;
    }
    .text-muted {
      color: #a8a8a8 !important;
    }
  </style>
</head>
<body>
<%- include('../partials/navbar') %>

<div class="page-header">
  <div class="container">
    <h1><i class="fas fa-rotate-left"></i> 🏁 REFUND MANAGEMENT</h1>
    <p class="mb-0" style="color: #ffd700; letter-spacing: 2px; font-weight: 600;">Process Returns & Manage Vehicle Refunds</p>
  </div>
</div>

<div class="container pb-5">
  <% if (typeof messages !== 'undefined') { %>
    <% if (messages.error && messages.error.length) { %>
      <div class="alert alert-danger alert-dismissible fade show">
        <i class="fas fa-exclamation-circle"></i> <%= messages.error.join ? messages.error.join(', ') : messages.error %>
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
      </div>
    <% } %>
    <% if (messages.success && messages.success.length) { %>
      <div class="alert alert-success alert-dismissible fade show">
        <i class="fas fa-check-circle"></i> <%= messages.success.join ? messages.success.join(', ') : messages.success %>
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
      </div>
    <% } %>
  <% } %>

  <div class="row mb-4">
    <div class="col-md-12">
      <div class="stats-overview">
        <div class="row text-center">
          <div class="col-md-3">
            <h5><%= orders.length %></h5>
            <small>Total Orders</small>
          </div>
          <div class="col-md-3">
            <h5 style="color: #00d4ff;"><%= orders.filter(o => o.refundStatus === 'none').length %></h5>
            <small>Not Refunded</small>
          </div>
          <div class="col-md-3">
            <h5 style="color: #ffd700;"><%= orders.filter(o => o.refundStatus === 'partial').length %></h5>
            <small>Partially Refunded</small>
          </div>
          <div class="col-md-3">
            <h5 style="color: #ff3333;"><%= orders.filter(o => o.refundStatus === 'full').length %></h5>
            <small>Fully Refunded</small>
          </div>
        </div>
      </div>
    </div>
  </div>

  <% if (orders.length === 0) { %>
    <div class="alert alert-info text-center">
      <i class="fas fa-info-circle fa-2x mb-3 d-block"></i>
      <h4>No Orders Found</h4>
      <p>There are no orders to display.</p>
    </div>
  <% } else { %>
    <% for (let order of orders) { %>
      <div class="order-card">
        <div class="order-header">
          <div>
            <h5><i class="fas fa-file-invoice"></i> <%= order.orderId %></h5>
            <small class="text-muted">
              <%= new Date(order.orderDate).toLocaleString('en-SG') %>
            </small>
          </div>
          <div>
            <% if (order.refundStatus === 'none') { %>
              <span class="badge badge-refund-none">Not Refunded</span>
            <% } else if (order.refundStatus === 'partial') { %>
              <span class="badge badge-refund-partial">Partially Refunded</span>
            <% } else { %>
              <span class="badge badge-refund-full">Fully Refunded</span>
            <% } %>
          </div>
        </div>

        <div class="row mb-3">
          <div class="col-md-6">
            <h6><i class="fas fa-user"></i> Customer Information</h6>
            <p class="mb-2"><strong style="color: #ffffff;"><%= order.userName %></strong></p>
            <p class="mb-2 text-muted"><i class="fas fa-envelope me-2"></i><%= order.userEmail %></p>
          </div>
          <div class="col-md-6">
            <h6><i class="fas fa-credit-card"></i> Payment Details</h6>
            <p class="mb-2">
              <% if (order.paymentProvider === 'PayPal') { %>
                <i class="fab fa-paypal" style="color: #00d4ff;"></i> PayPal
              <% } else if (order.paymentProvider === 'Stripe') { %>
                <i class="fab fa-stripe" style="color: #a855f7;"></i> Stripe
              <% } else if (order.paymentProvider === 'NETS') { %>
                <i class="fas fa-qrcode" style="color: #ffd700;"></i> NETS
              <% } else if (order.paymentProvider === 'Wallet') { %>
                <i class="fas fa-wallet" style="color: #ff3333;"></i> Wallet
              <% } else { %>
                <i class="fas fa-credit-card"></i> <%= order.paymentProvider || 'N/A' %>
              <% } %>
            </p>
            <% if (order.paymentReference) { %>
              <small class="text-muted">Ref: <%= order.paymentReference.substring(0, 25) %>...</small>
            <% } %>
          </div>
        </div>

        <hr>

        <div class="row">
          <div class="col-md-8">
            <h6><i class="fas fa-car"></i> Vehicle Items</h6>
            <ul class="list-unstyled">
              <% for (let item of order.items) { %>
                <li class="mb-2" style="color: #ffffff;">
                  <i class="fas fa-angle-right me-2" style="color: #ff3333;"></i>
                  <strong><%= item.productName %></strong> × <%= item.quantity %> 
                  <span style="color: #ffd700;">- $<%= (item.price * item.quantity).toFixed(2) %></span>
                </li>
              <% } %>
            </ul>
          </div>
          <div class="col-md-4">
            <h6><i class="fas fa-calculator"></i> Order Totals</h6>
            <div class="d-flex justify-content-between mb-2">
              <span>Subtotal:</span>
              <span style="color: #ffffff;">$<%= order.subtotal.toFixed(2) %></span>
            </div>
            <div class="d-flex justify-content-between mb-2">
              <span>Tax:</span>
              <span style="color: #ffffff;">$<%= order.tax.toFixed(2) %></span>
            </div>
            <div class="d-flex justify-content-between mb-2 fw-bold">
              <span style="color: #ffd700;">Total:</span>
              <span style="color: #ffd700; font-size: 1.3rem;">$<%= order.total.toFixed(2) %></span>
            </div>
            <% if (order.refundedAmount > 0) { %>
              <div class="d-flex justify-content-between mb-2" style="color: #ff3333;">
                <span>Refunded:</span>
                <span>-$<%= order.refundedAmount.toFixed(2) %></span>
              </div>
            <% } %>
          </div>
        </div>

        <hr>

        <div class="refund-actions">
          <div style="background: rgba(0, 188, 212, 0.2); border-left: 4px solid #00bcd4; padding: 15px; border-radius: 8px;">
            <i class="fas fa-info-circle" style="color: #00bcd4;"></i>
            <span style="color: #ffffff; margin-left: 10px; font-weight: 600;">
              Refunds are managed by sellers. Contact the seller for refund processing.
            </span>
          </div>
            </button>
          <% } else { %>
            <span class="text-muted"><i class="fas fa-info-circle"></i> Manual refund required for <%= order.paymentProvider || 'this payment method' %></span>
          <% } %>
        </div>
      </div>
    <% } %>
  <% } %>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

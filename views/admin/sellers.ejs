<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Manage Sellers - Car Dealership</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link rel="stylesheet" href="/theme.css">
    <style>
        body {
            background: linear-gradient(135deg, #0a0a0a 0%, #1a1a1a 100%);
            color: #ffffff;
            min-height: 100vh;
        }

        .admin-header {
            background: linear-gradient(135deg, #000000 0%, #ff3333 50%, #000000 100%);
            padding: 3rem 0;
            margin-bottom: 2.5rem;
            box-shadow: 0 15px 50px rgba(255, 51, 51, 0.4);
            border-bottom: 4px solid #ff3333;
        }

        .admin-header h1 {
            font-weight: 900;
            text-transform: uppercase;
            letter-spacing: 4px;
            text-shadow: 0 0 30px rgba(255, 51, 51, 0.8);
            margin-bottom: 0.5rem;
        }

        .admin-header p {
            opacity: 0.9;
            font-size: 1.1rem;
        }

        .seller-card {
            background: linear-gradient(135deg, #1a1a1a 0%, #0a0a0a 100%);
            border-radius: 15px;
            padding: 2rem;
            margin-bottom: 1.5rem;
            border: 2px solid #333;
            transition: all 0.3s ease;
            box-shadow: 0 10px 30px rgba(0,0,0,0.6);
        }

        .seller-card:hover {
            border-color: #ff3333;
            box-shadow: 0 15px 40px rgba(255, 51, 51, 0.3);
            transform: translateY(-2px);
        }

        .status-badge {
            padding: 0.5rem 1.2rem;
            border-radius: 25px;
            font-weight: 700;
            font-size: 0.9rem;
            text-transform: uppercase;
            letter-spacing: 1px;
            box-shadow: 0 4px 15px rgba(0,0,0,0.3);
        }

        .status-pending {
            background: linear-gradient(135deg, #ffa500 0%, #ff8c00 100%);
            color: #000;
        }

        .status-approved {
            background: linear-gradient(135deg, #00ff00 0%, #00cc00 100%);
            color: #000;
        }

        .status-rejected {
            background: linear-gradient(135deg, #ff0000 0%, #cc0000 100%);
            color: #fff;
        }

        .status-suspended {
            background: linear-gradient(135deg, #666 0%, #444 100%);
            color: #fff;
        }

        .info-label {
            color: #ff3333;
            font-weight: 700;
            margin-bottom: 0.3rem;
            font-size: 0.85rem;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .info-value {
            color: #ffffff;
            margin-bottom: 1.2rem;
            font-size: 1rem;
        }

        .action-btn {
            margin: 0.3rem;
            border: none;
            padding: 0.6rem 1.5rem;
            border-radius: 8px;
            font-weight: 600;
            transition: all 0.3s ease;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .btn-success {
            background: linear-gradient(135deg, #00ff00 0%, #00cc00 100%);
            color: #000;
        }

        .btn-success:hover {
            background: linear-gradient(135deg, #00cc00 0%, #009900 100%);
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(0, 255, 0, 0.4);
        }

        .btn-danger {
            background: linear-gradient(135deg, #ff3333 0%, #cc0000 100%);
            color: #fff;
        }

        .btn-danger:hover {
            background: linear-gradient(135deg, #cc0000 0%, #990000 100%);
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(255, 51, 51, 0.4);
        }

        .btn-warning {
            background: linear-gradient(135deg, #ffa500 0%, #ff8c00 100%);
            color: #000;
        }

        .btn-warning:hover {
            background: linear-gradient(135deg, #ff8c00 0%, #ff6600 100%);
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(255, 165, 0, 0.4);
        }

        .nav-tabs {
            border-bottom: 3px solid #333;
            margin-bottom: 2rem;
        }

        .nav-tabs .nav-link {
            color: #999;
            border: none;
            background: transparent;
            margin-right: 1rem;
            padding: 1rem 2rem;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 1px;
            transition: all 0.3s ease;
            border-radius: 10px 10px 0 0;
        }

        .nav-tabs .nav-link:hover {
            color: #ff3333;
            background: rgba(255, 51, 51, 0.1);
        }

        .nav-tabs .nav-link.active {
            background: linear-gradient(135deg, #ff3333 0%, #cc0000 100%);
            color: #fff;
            box-shadow: 0 -5px 20px rgba(255, 51, 51, 0.3);
        }

        .tab-content {
            margin-top: 2rem;
        }

        .alert {
            border-radius: 10px;
            border: none;
            padding: 1rem 1.5rem;
            font-weight: 600;
            box-shadow: 0 5px 15px rgba(0,0,0,0.3);
        }

        .alert-success {
            background: linear-gradient(135deg, #00ff00 0%, #00cc00 100%);
            color: #000;
        }

        .alert-danger {
            background: linear-gradient(135deg, #ff3333 0%, #cc0000 100%);
            color: #fff;
        }

        .empty-state {
            text-align: center;
            padding: 4rem 2rem;
            color: #666;
        }

        .empty-state i {
            font-size: 4rem;
            margin-bottom: 1rem;
            color: #333;
        }

        .btn-secondary {
            background: linear-gradient(135deg, #333 0%, #1a1a1a 100%);
            color: #ffffff;
            border: 2px solid #555;
        }

        .btn-secondary:hover {
            background: linear-gradient(135deg, #555 0%, #333 100%);
            color: #ffffff;
            border-color: #ff3333;
            box-shadow: 0 5px 15px rgba(255, 51, 51, 0.3);
        }
    </style>
</head>
<body>
    <%- include('../partials/navbar') %>

    <div class="admin-header">
        <div class="container">
            <h1><i class="fas fa-users-cog"></i> Manage Sellers</h1>
            <p class="mb-0">Review and manage seller applications</p>
        </div>
    </div>

    <div class="container">
        <%- include('../partials/messages') %>

        <!-- Statistics -->
        <div class="row mb-4">
            <div class="col-md-3">
                <div class="seller-card text-center">
                    <h3 class="mb-0"><%= sellers.filter(s => s.seller_status === 'pending').length %></h3>
                    <p class="mb-0 text-muted">Pending</p>
                </div>
            </div>
            <div class="col-md-3">
                <div class="seller-card text-center">
                    <h3 class="mb-0"><%= sellers.filter(s => s.seller_status === 'approved').length %></h3>
                    <p class="mb-0 text-muted">Approved</p>
                </div>
            </div>
            <div class="col-md-3">
                <div class="seller-card text-center">
                    <h3 class="mb-0"><%= sellers.filter(s => s.seller_status === 'rejected').length %></h3>
                    <p class="mb-0 text-muted">Rejected</p>
                </div>
            </div>
            <div class="col-md-3">
                <div class="seller-card text-center">
                    <h3 class="mb-0"><%= sellers.filter(s => s.seller_status === 'suspended').length %></h3>
                    <p class="mb-0 text-muted">Suspended</p>
                </div>
            </div>
        </div>

        <!-- Tabs -->
        <ul class="nav nav-tabs" role="tablist">
            <li class="nav-item" role="presentation">
                <button class="nav-link active" data-bs-toggle="tab" data-bs-target="#pending" type="button">
                    <i class="fas fa-clock"></i> Pending (<%= sellers.filter(s => s.seller_status === 'pending').length %>)
                </button>
            </li>
            <li class="nav-item" role="presentation">
                <button class="nav-link" data-bs-toggle="tab" data-bs-target="#approved" type="button">
                    <i class="fas fa-check-circle"></i> Approved (<%= sellers.filter(s => s.seller_status === 'approved').length %>)
                </button>
            </li>
            <li class="nav-item" role="presentation">
                <button class="nav-link" data-bs-toggle="tab" data-bs-target="#rejected" type="button">
                    <i class="fas fa-times-circle"></i> Rejected (<%= sellers.filter(s => s.seller_status === 'rejected').length %>)
                </button>
            </li>
            <li class="nav-item" role="presentation">
                <button class="nav-link" data-bs-toggle="tab" data-bs-target="#suspended" type="button">
                    <i class="fas fa-ban"></i> Suspended (<%= sellers.filter(s => s.seller_status === 'suspended').length %>)
                </button>
            </li>
        </ul>

        <!-- Tab Content -->
        <div class="tab-content">
            <!-- Pending Tab -->
            <div class="tab-pane fade show active" id="pending">
                <% const pendingSellers = sellers.filter(s => s.seller_status === 'pending'); %>
                <% if (pendingSellers.length === 0) { %>
                    <div class="seller-card text-center">
                        <i class="fas fa-inbox fa-3x mb-3" style="color: #666;"></i>
                        <p class="text-muted">No pending applications</p>
                    </div>
                <% } else { %>
                    <% pendingSellers.forEach(seller => { %>
                        <div class="seller-card">
                            <div class="row">
                                <div class="col-md-8">
                                    <div class="d-flex align-items-center mb-3">
                                        <h4 class="mb-0 me-3"><%= seller.username %></h4>
                                        <span class="status-badge status-pending">
                                            <i class="fas fa-clock"></i> Pending Review
                                        </span>
                                    </div>

                                    <div class="row">
                                        <div class="col-md-6">
                                            <div class="info-label">Email</div>
                                            <div class="info-value"><%= seller.email %></div>

                                            <div class="info-label">Business Name</div>
                                            <div class="info-value"><%= seller.business_name || 'N/A' %></div>

                                            <div class="info-label">Phone</div>
                                            <div class="info-value"><%= seller.business_phone || 'N/A' %></div>
                                        </div>
                                        <div class="col-md-6">
                                            <div class="info-label">Address</div>
                                            <div class="info-value"><%= seller.business_address || 'N/A' %></div>

                                            <div class="info-label">Applied On</div>
                                            <div class="info-value"><%= new Date(seller.created_at).toLocaleDateString() %></div>
                                        </div>
                                    </div>

                                    <div class="info-label">Business Description</div>
                                    <div class="info-value"><%= seller.business_description || 'N/A' %></div>
                                </div>
                                <div class="col-md-4 d-flex flex-column justify-content-center">
                                    <form method="POST" action="/admin/sellers/<%= seller.id %>/approve" class="mb-2">
                                        <button type="submit" class="btn btn-success w-100 action-btn">
                                            <i class="fas fa-check"></i> Approve
                                        </button>
                                    </form>
                                    <form method="POST" action="/admin/sellers/<%= seller.id %>/reject">
                                        <button type="submit" class="btn btn-danger w-100 action-btn">
                                            <i class="fas fa-times"></i> Reject
                                        </button>
                                    </form>
                                </div>
                            </div>
                        </div>
                    <% }); %>
                <% } %>
            </div>

            <!-- Approved Tab -->
            <div class="tab-pane fade" id="approved">
                <% const approvedSellers = sellers.filter(s => s.seller_status === 'approved'); %>
                <% if (approvedSellers.length === 0) { %>
                    <div class="seller-card text-center">
                        <i class="fas fa-inbox fa-3x mb-3" style="color: #666;"></i>
                        <p class="text-muted">No approved sellers</p>
                    </div>
                <% } else { %>
                    <% approvedSellers.forEach(seller => { %>
                        <div class="seller-card">
                            <div class="row">
                                <div class="col-md-9">
                                    <div class="d-flex align-items-center mb-3">
                                        <h4 class="mb-0 me-3"><%= seller.username %></h4>
                                        <span class="status-badge status-approved">
                                            <i class="fas fa-check-circle"></i> Approved
                                        </span>
                                    </div>

                                    <div class="row">
                                        <div class="col-md-6">
                                            <div class="info-label">Email</div>
                                            <div class="info-value"><%= seller.email %></div>

                                            <div class="info-label">Business Name</div>
                                            <div class="info-value"><%= seller.business_name || 'N/A' %></div>
                                        </div>
                                        <div class="col-md-6">
                                            <div class="info-label">Phone</div>
                                            <div class="info-value"><%= seller.business_phone || 'N/A' %></div>

                                            <div class="info-label">Approved On</div>
                                            <div class="info-value"><%= seller.seller_approved_at ? new Date(seller.seller_approved_at).toLocaleDateString() : 'N/A' %></div>
                                        </div>
                                    </div>
                                </div>
                                <div class="col-md-3 d-flex flex-column justify-content-center">
                                    <form method="POST" action="/admin/sellers/<%= seller.id %>/suspend">
                                        <button type="submit" class="btn btn-warning w-100 action-btn">
                                            <i class="fas fa-ban"></i> Suspend
                                        </button>
                                    </form>
                                </div>
                            </div>
                        </div>
                    <% }); %>
                <% } %>
            </div>

            <!-- Rejected Tab -->
            <div class="tab-pane fade" id="rejected">
                <% const rejectedSellers = sellers.filter(s => s.seller_status === 'rejected'); %>
                <% if (rejectedSellers.length === 0) { %>
                    <div class="seller-card text-center">
                        <i class="fas fa-inbox fa-3x mb-3" style="color: #666;"></i>
                        <p class="text-muted">No rejected applications</p>
                    </div>
                <% } else { %>
                    <% rejectedSellers.forEach(seller => { %>
                        <div class="seller-card">
                            <div class="d-flex align-items-center mb-3">
                                <h4 class="mb-0 me-3"><%= seller.username %></h4>
                                <span class="status-badge status-rejected">
                                    <i class="fas fa-times-circle"></i> Rejected
                                </span>
                            </div>

                            <div class="row">
                                <div class="col-md-6">
                                    <div class="info-label">Email</div>
                                    <div class="info-value"><%= seller.email %></div>

                                    <div class="info-label">Business Name</div>
                                    <div class="info-value"><%= seller.business_name || 'N/A' %></div>
                                </div>
                                <div class="col-md-6">
                                    <div class="info-label">Phone</div>
                                    <div class="info-value"><%= seller.business_phone || 'N/A' %></div>

                                    <div class="info-label">Applied On</div>
                                    <div class="info-value"><%= new Date(seller.created_at).toLocaleDateString() %></div>
                                </div>
                            </div>
                        </div>
                    <% }); %>
                <% } %>
            </div>

            <!-- Suspended Tab -->
            <div class="tab-pane fade" id="suspended">
                <% const suspendedSellers = sellers.filter(s => s.seller_status === 'suspended'); %>
                <% if (suspendedSellers.length === 0) { %>
                    <div class="seller-card text-center">
                        <i class="fas fa-inbox fa-3x mb-3" style="color: #666;"></i>
                        <p class="text-muted">No suspended sellers</p>
                    </div>
                <% } else { %>
                    <% suspendedSellers.forEach(seller => { %>
                        <div class="seller-card">
                            <div class="row">
                                <div class="col-md-9">
                                    <div class="d-flex align-items-center mb-3">
                                        <h4 class="mb-0 me-3"><%= seller.username %></h4>
                                        <span class="status-badge status-suspended">
                                            <i class="fas fa-ban"></i> Suspended
                                        </span>
                                    </div>

                                    <div class="row">
                                        <div class="col-md-6">
                                            <div class="info-label">Email</div>
                                            <div class="info-value"><%= seller.email %></div>

                                            <div class="info-label">Business Name</div>
                                            <div class="info-value"><%= seller.business_name || 'N/A' %></div>
                                        </div>
                                        <div class="col-md-6">
                                            <div class="info-label">Phone</div>
                                            <div class="info-value"><%= seller.business_phone || 'N/A' %></div>
                                        </div>
                                    </div>
                                </div>
                                <div class="col-md-3 d-flex flex-column justify-content-center">
                                    <form method="POST" action="/admin/sellers/<%= seller.id %>/reactivate">
                                        <button type="submit" class="btn btn-success w-100 action-btn">
                                            <i class="fas fa-check"></i> Reactivate
                                        </button>
                                    </form>
                                </div>
                            </div>
                        </div>
                    <% }); %>
                <% } %>
            </div>
        </div>

        <div class="text-center mt-4">
            <a href="/admin/dashboard" class="btn btn-secondary">
                <i class="fas fa-arrow-left"></i> Back to Dashboard
            </a>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

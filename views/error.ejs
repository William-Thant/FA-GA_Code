<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= statusCode %> - <%= message %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
  <style>
    body { background-color: #f8f9fa; }
    .error-container { 
      min-height: 80vh; 
      display: flex; 
      align-items: center; 
      justify-content: center; 
    }
    .error-card { 
      background: white; 
      border-radius: 15px; 
      box-shadow: 0 10px 30px rgba(0,0,0,0.1); 
      padding: 60px 40px; 
      text-align: center; 
      max-width: 600px; 
    }
    .error-icon { 
      font-size: 6rem; 
      color: <%= statusCode === 404 ? '#ffc107' : '#dc3545' %>; 
      margin-bottom: 30px; 
    }
    .error-code { 
      font-size: 5rem; 
      font-weight: bold; 
      color: #667eea; 
      line-height: 1; 
      margin-bottom: 20px; 
    }
    .error-message { 
      font-size: 1.8rem; 
      font-weight: 600; 
      color: #333; 
      margin-bottom: 15px; 
    }
    .error-description { 
      color: #6c757d; 
      font-size: 1.1rem; 
      margin-bottom: 40px; 
    }
  </style>
</head>
<body>
<% if (typeof user !== 'undefined' && user) { %>
  <%- include('partials/navbar') %>
<% } else { %>
  <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">Kyan's Supermarket</a>
    </div>
  </nav>
<% } %>

<div class="error-container">
  <div class="error-card">
    <div class="error-icon">
      <i class="fas fa-<%= statusCode === 404 ? 'exclamation-triangle' : 'times-circle' %>"></i>
    </div>
    <div class="error-code"><%= statusCode %></div>
    <div class="error-message"><%= message %></div>
    <div class="error-description"><%= description %></div>
    <div class="d-grid gap-2 d-md-flex justify-content-md-center">
      <a href="<%= (typeof user !== 'undefined' && user) ? (user.role === 'admin' ? '/admin/dashboard' : '/shop') : '/' %>" class="btn btn-primary btn-lg">
        <i class="fas fa-home"></i> Go to Home
      </a>
      <button onclick="history.back()" class="btn btn-outline-secondary btn-lg">
        <i class="fas fa-arrow-left"></i> Go Back
      </button>
    </div>
  </div>
</div>

</body>
</html>

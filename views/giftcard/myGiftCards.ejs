<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My Gift Cards - Isaac's Cardealership</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <link rel="stylesheet" href="/theme.css">
  <style>
    body { 
      background: linear-gradient(135deg, #0a0a0a 0%, #1a1a1a 100%);
      min-height: 100vh;
      color: #ffffff;
    }
    .page-header { 
      background: linear-gradient(135deg, #000000 0%, #1a1a1a 50%, #ff3333 100%);
      color: white; 
      padding: 60px 0; 
      margin-bottom: 40px;
      border-bottom: 3px solid #ff3333;
      box-shadow: 0 10px 40px rgba(255, 51, 51, 0.3);
    }
    .page-header h1 {
      font-weight: 800;
      text-transform: uppercase;
      letter-spacing: 3px;
      font-size: 2.5rem;
      text-shadow: 0 0 20px rgba(255, 51, 51, 0.5);
    }
    .page-header p {
      color: #a8a8a8;
      font-size: 1.1rem;
      letter-spacing: 1px;
    }
    .gift-card-item {
      background: linear-gradient(135deg, #1a1a1a 0%, #0a0a0a 100%);
      border: 2px solid #333;
      border-radius: 15px;
      padding: 30px;
      margin-bottom: 30px;
      box-shadow: 0 10px 30px rgba(0,0,0,0.5);
      transition: all 0.3s ease;
    }
    .gift-card-item:hover {
      transform: translateY(-8px);
      box-shadow: 0 15px 50px rgba(255, 51, 51, 0.4);
      border-color: #ff3333;
    }
    .card-status {
      display: inline-block;
      padding: 8px 20px;
      border-radius: 25px;
      font-size: 0.9rem;
      font-weight: 800;
      text-transform: uppercase;
      letter-spacing: 1px;
    }
    .status-active {
      background: linear-gradient(135deg, #00d4ff 0%, #0099cc 100%);
      color: white;
      box-shadow: 0 0 20px rgba(0, 212, 255, 0.3);
    }
    .status-redeemed {
      background: linear-gradient(135deg, #ffd700 0%, #cc9900 100%);
      color: #000;
      box-shadow: 0 0 20px rgba(255, 215, 0, 0.3);
    }
    .status-expired {
      background: linear-gradient(135deg, #ff3333 0%, #990000 100%);
      color: white;
      box-shadow: 0 0 20px rgba(255, 51, 51, 0.3);
    }
    .gift-code {
      font-family: 'Courier New', monospace;
      font-size: 1.3rem;
      font-weight: 800;
      color: #00d4ff;
      letter-spacing: 2px;
      text-shadow: 0 0 10px rgba(0, 212, 255, 0.5);
    }
    .balance-display {
      font-size: 2rem;
      font-weight: 800;
      color: #ffd700;
      text-shadow: 0 0 20px rgba(255, 215, 0, 0.5);
    }
    .empty-state {
      background: linear-gradient(135deg, #1a1a1a 0%, #0a0a0a 100%);
      border: 2px solid #333;
      border-radius: 15px;
      text-align: center;
      padding: 80px 40px;
      box-shadow: 0 10px 30px rgba(0,0,0,0.5);
    }
    .empty-state i {
      font-size: 6rem;
      color: #ff3333;
      margin-bottom: 30px;
      filter: drop-shadow(0 0 20px rgba(255, 51, 51, 0.5));
    }
    .empty-state h3 {
      color: #ffffff;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 2px;
      font-size: 1.8rem;
    }
    .empty-state p {
      color: #a8a8a8;
      font-size: 1.1rem;
    }
    .btn-primary {
      background: linear-gradient(135deg, #ff3333 0%, #cc0000 100%);
      border: 2px solid #ff3333;
      color: white !important;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 1px;
      padding: 12px 30px;
      border-radius: 25px;
      transition: all 0.3s ease;
    }
    .btn-primary:hover {
      background: linear-gradient(135deg, #cc0000 0%, #990000 100%);
      transform: scale(1.05);
      box-shadow: 0 5px 20px rgba(255, 51, 51, 0.5);
    }
    .btn-success {
      background: linear-gradient(135deg, #00d4ff 0%, #0099cc 100%);
      border: 2px solid #00d4ff;
      color: white !important;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 1px;
      transition: all 0.3s ease;
    }
    .btn-success:hover {
      background: linear-gradient(135deg, #0099cc 0%, #007799 100%);
      transform: scale(1.05);
      box-shadow: 0 5px 20px rgba(0, 212, 255, 0.5);
    }
    .btn-outline-secondary {
      border: 2px solid #333;
      color: #ffffff !important;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 1px;
      background: rgba(0, 0, 0, 0.3);
      padding: 12px 30px;
      border-radius: 25px;
      transition: all 0.3s ease;
    }
    .btn-outline-secondary:hover {
      background: linear-gradient(135deg, #ff3333 0%, #cc0000 100%);
      border-color: #ff3333;
      color: white !important;
      transform: scale(1.05);
      box-shadow: 0 5px 20px rgba(255, 51, 51, 0.5);
    }
    .btn-outline-primary {
      border: 2px solid #00d4ff;
      color: #00d4ff !important;
      font-weight: 700;
      text-transform: uppercase;
      background: transparent;
      transition: all 0.3s ease;
    }
    .btn-outline-primary:hover {
      background: linear-gradient(135deg, #00d4ff 0%, #0099cc 100%);
      border-color: #00d4ff;
      color: white !important;
      box-shadow: 0 5px 20px rgba(0, 212, 255, 0.5);
    }
    .text-muted {
      color: #a8a8a8 !important;
    }
    .fw-bold {
      color: #ffffff;
      font-size: 1.1rem;
    }
    hr {
      border-color: #333 !important;
      opacity: 1;
    }
    .badge {
      padding: 8px 15px;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 1px;
    }
    .bg-info {
      background: linear-gradient(135deg, #ffd700 0%, #cc9900 100%) !important;
      color: #000 !important;
    }
    .alert-danger {
      background: rgba(255, 51, 51, 0.1);
      border: 2px solid #ff3333;
      color: #ff3333;
    }
    .alert-success {
      background: rgba(0, 212, 255, 0.1);
      border: 2px solid #00d4ff;
      color: #00d4ff;
    }
  </style>
</head>
<body>
  <%- include('../partials/navbar') %>
  
  <div class="page-header">
    <div class="container">
      <h1 class="text-center"><i class="fas fa-gift me-2"></i>MY GIFT CARDS</h1>
      <p class="text-center mb-0">Manage your purchased gift cards</p>
    </div>
  </div>

  <div class="container my-5">
    <% if (messages.error && messages.error.length > 0) { %>
      <div class="alert alert-danger alert-dismissible fade show" role="alert">
        <%= messages.error[0] %>
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
      </div>
    <% } %>

    <% if (messages.success && messages.success.length > 0) { %>
      <div class="alert alert-success alert-dismissible fade show" role="alert">
        <%= messages.success[0] %>
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
      </div>
    <% } %>

    <div class="mb-4">
      <a href="/giftcards/purchase" class="btn btn-primary">
        <i class="fas fa-plus me-2"></i>Purchase New Gift Card
      </a>
      <a href="/giftcards/balance" class="btn btn-outline-secondary">
        <i class="fas fa-search me-2"></i>Check Balance
      </a>
    </div>

    <% if (giftCards && giftCards.length > 0) { %>
      <div class="row">
        <% giftCards.forEach(card => { 
          const status = card.status;
          const isExpired = new Date(card.expiryDate) < new Date();
          const statusClass = status === 'active' ? 'status-active' : status === 'redeemed' ? 'status-redeemed' : 'status-expired';
          const statusIcon = status === 'active' ? 'fa-check-circle' : status === 'redeemed' ? 'fa-gift' : 'fa-times-circle';
        %>
        <div class="col-lg-6">
          <div class="gift-card-item">
            <div class="d-flex justify-content-between align-items-start mb-3">
              <div>
                <span class="card-status <%= statusClass %>">
                  <i class="fas <%= statusIcon %> me-1"></i>
                  <%= status.toUpperCase() %>
                </span>
              </div>
              <div class="text-end">
                <small class="text-muted">
                  <i class="fas fa-calendar me-1"></i>
                  <%= new Date(card.createdAt).toLocaleDateString() %>
                </small>
              </div>
            </div>

            <div class="row align-items-center">
              <div class="col-md-7">
                <div class="mb-2">
                  <small class="text-muted">Gift Card Code</small>
                  <div class="gift-code"><%= card.code %></div>
                </div>
                
                <div class="mb-2">
                  <small class="text-muted">Original Amount</small>
                  <div class="fw-bold">$<%= parseFloat(card.initialAmount).toFixed(2) %></div>
                </div>

                <% if (card.recipientEmail) { %>
                <div class="mb-2">
                  <small class="text-muted">Sent To</small>
                  <div><i class="fas fa-envelope me-2"></i><%= card.recipientEmail %></div>
                </div>
                <% } %>
              </div>

              <div class="col-md-5 text-center">
                <small class="text-muted">Current Balance</small>
                <div class="balance-display">$<%= parseFloat(card.currentBalance).toFixed(2) %></div>
                
                <% if (status === 'active' && parseFloat(card.currentBalance) > 0) { %>
                  <button class="btn btn-sm btn-success mt-2" onclick="copyCode('<%= card.code %>')">
                    <i class="fas fa-copy me-1"></i>Copy Code
                  </button>
                <% } %>
              </div>
            </div>

            <hr>

            <div class="d-flex justify-content-between align-items-center">
              <small class="text-muted">
                <i class="fas fa-clock me-1"></i>
                Expires: <%= new Date(card.expiryDate).toLocaleDateString() %>
              </small>
              
              <% if (status === 'active' && parseFloat(card.currentBalance) > 0) { %>
                <a href="/shop" class="btn btn-sm btn-outline-primary">
                  <i class="fas fa-car me-1"></i>Use Now
                </a>
              <% } else if (status === 'redeemed') { %>
                <span class="badge bg-info">
                  <i class="fas fa-check me-1"></i>Fully Redeemed
                </span>
              <% } %>
            </div>
          </div>
        </div>
        <% }); %>
      </div>
    <% } else { %>
      <div class="empty-state">
        <i class="fas fa-gift"></i>
        <h3>No Gift Cards Yet</h3>
        <p class="text-muted mb-4">You haven't purchased any gift cards yet. Start by purchasing your first gift card!</p>
        <a href="/giftcards/purchase" class="btn btn-primary btn-lg">
          <i class="fas fa-plus me-2"></i>Purchase Gift Card
        </a>
      </div>
    <% } %>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <script>
    function copyCode(code) {
      navigator.clipboard.writeText(code).then(() => {
        const btn = event.target.closest('button');
        const originalText = btn.innerHTML;
        btn.innerHTML = '<i class="fas fa-check me-1"></i>Copied!';
        btn.classList.add('btn-info');
        btn.classList.remove('btn-success');
        
        setTimeout(() => {
          btn.innerHTML = originalText;
          btn.classList.remove('btn-info');
          btn.classList.add('btn-success');
        }, 2000);
      }).catch(err => {
        alert('Failed to copy code. Please copy it manually: ' + code);
      });
    }
  </script>
</body>
</html>

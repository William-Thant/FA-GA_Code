<!DOCTYPE html>
<html lang="en" data-bs-theme="dark">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
  <link rel="stylesheet" href="/theme.css">
  <title>Vehicle Inventory - Isaac's Car Dealership</title>
  <style>
    body {
      background: linear-gradient(135deg, #0a0a0a 0%, #1a1a1a 100%);
      min-height: 100vh;
      color: #ffffff;
    }

    .inventory-header {
      background: linear-gradient(135deg, #000000 0%, #1a1a1a 50%, #ff3333 100%);
      color: white;
      padding: 60px 0;
      margin-bottom: 40px;
      border-bottom: 3px solid #ff3333;
      box-shadow: 0 10px 40px rgba(255, 51, 51, 0.3);
    }

    .inventory-header h1 {
      font-weight: 800;
      text-transform: uppercase;
      letter-spacing: 3px;
      font-size: 2.5rem;
      text-shadow: 0 0 20px rgba(255, 51, 51, 0.5);
    }

    .inventory-header p {
      color: #a8a8a8;
      font-size: 1.1rem;
      letter-spacing: 1px;
      margin-bottom: 0;
    }

    .inventory-card {
      background: linear-gradient(135deg, #1a1a1a 0%, #0a0a0a 100%);
      border: 2px solid #333;
      border-radius: 15px;
      box-shadow: 0 10px 30px rgba(0,0,0,0.5);
      padding: 0;
      margin-bottom: 30px;
      overflow: hidden;
      transition: all 0.3s ease;
    }

    .inventory-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 15px 50px rgba(255, 51, 51, 0.3);
      border-color: #ff3333;
    }

    .table-responsive {
      overflow-x: auto;
    }

    .inventory-table {
      margin: 0;
      width: 100%;
      background: transparent;
    }

    .inventory-table thead {
      background: linear-gradient(135deg, #ff3333 0%, #cc0000 100%);
    }

    .inventory-table thead th {
      color: #ffffff;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 1px;
      padding: 1.2rem 1rem;
      border: none;
      font-size: 0.9rem;
    }

    .inventory-table tbody tr {
      background: #1a1a1a;
      border-bottom: 1px solid #333;
      transition: all 0.3s ease;
    }

    .inventory-table tbody tr:hover {
      background: linear-gradient(135deg, #2d2d2d 0%, #1a1a1a 100%);
      transform: translateX(5px);
      box-shadow: inset 5px 0 0 #ff3333;
    }

    .inventory-table tbody td {
      padding: 1rem;
      vertical-align: middle;
      color: #e0e0e0;
      border: none;
    }

    .inventory-table tbody td img {
      border-radius: 8px;
      border: 2px solid #333;
      transition: all 0.3s ease;
    }

    .inventory-table tbody td img:hover {
      border-color: #ff3333;
      transform: scale(1.1);
    }

    .inventory-table tbody td a {
      color: #ff3333;
      text-decoration: none;
      font-weight: 600;
      transition: all 0.3s ease;
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
    }

    .inventory-table tbody td a:hover {
      color: #ff6666;
      text-shadow: 0 0 10px rgba(255, 51, 51, 0.5);
    }

    .action-link {
      padding: 0.5rem 1rem;
      background: linear-gradient(135deg, #ff3333 0%, #cc0000 100%);
      color: white;
      border-radius: 8px;
      text-decoration: none;
      transition: all 0.3s ease;
      display: inline-block;
      font-weight: 600;
      font-size: 0.85rem;
    }

    .action-link:hover {
      background: linear-gradient(135deg, #cc0000 0%, #990000 100%);
      transform: translateY(-2px);
      box-shadow: 0 5px 15px rgba(255, 51, 51, 0.4);
      color: white;
    }

    .action-link.delete {
      background: linear-gradient(135deg, #6c757d 0%, #495057 100%);
    }

    .action-link.delete:hover {
      background: linear-gradient(135deg, #495057 0%, #343a40 100%);
    }

    .navbar-dark {
      background: linear-gradient(135deg, #1a1a1a 0%, #000000 100%) !important;
      border-bottom: 2px solid #ff3333;
      box-shadow: 0 4px 10px rgba(0,0,0,0.5);
    }

    .navbar-brand {
      font-weight: 800;
      color: #ff3333 !important;
      text-transform: uppercase;
      letter-spacing: 2px;
    }

    .nav-link {
      color: #e0e0e0 !important;
      font-weight: 600;
      transition: all 0.3s ease;
      text-transform: uppercase;
      font-size: 0.9rem;
      letter-spacing: 1px;
    }

    .nav-link:hover {
      color: #ff3333 !important;
      text-shadow: 0 0 10px rgba(255, 51, 51, 0.5);
    }

    .welcome-badge {
      background: linear-gradient(135deg, #1a1a1a 0%, #0a0a0a 100%);
      border: 2px solid #ff3333;
      border-radius: 10px;
      padding: 1rem 1.5rem;
      display: inline-block;
      margin-bottom: 2rem;
      box-shadow: 0 5px 15px rgba(255, 51, 51, 0.2);
    }

    .welcome-badge .username {
      color: #ff3333;
      font-weight: 700;
      font-size: 1.2rem;
    }

    .welcome-badge .role {
      color: #a8a8a8;
      font-weight: 500;
      text-transform: uppercase;
      font-size: 0.9rem;
      letter-spacing: 1px;
    }

    .btn-add {
      background: linear-gradient(135deg, #ff3333 0%, #cc0000 100%);
      color: white;
      border: none;
      padding: 0.75rem 2rem;
      border-radius: 10px;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 1px;
      transition: all 0.3s ease;
      box-shadow: 0 5px 15px rgba(255, 51, 51, 0.3);
    }

    .btn-add:hover {
      background: linear-gradient(135deg, #cc0000 0%, #990000 100%);
      transform: translateY(-3px);
      box-shadow: 0 8px 20px rgba(255, 51, 51, 0.5);
      color: white;
    }
  </style>
</head>
<body>
    <nav class="navbar navbar-expand-sm navbar-dark">
      <div class="container-fluid">
        <a class="navbar-brand" href="/">
          <i class="fas fa-car-side me-2"></i>Isaac's Car Dealership
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="collapsibleNavbar">
          <ul class="navbar-nav ms-auto">
            <li class="nav-item">
              <a class="nav-link" href="/admin/dashboard">
                <i class="fas fa-dashboard me-1"></i>Dashboard
              </a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/addProduct">
                <i class="fas fa-plus-circle me-1"></i>Add Vehicle
              </a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/shop">
                <i class="fas fa-store me-1"></i>Showroom
              </a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/logout">
                <i class="fas fa-sign-out-alt me-1"></i>Logout
              </a>
            </li>
          </ul>
        </div>
      </div>
    </nav>

    <div class="inventory-header">
      <div class="container text-center">
        <h1><i class="fas fa-warehouse me-3"></i>Vehicle Inventory</h1>
        <p>Manage your dealership's vehicle stock and pricing</p>
      </div>
    </div>

    <div class="container">
      <div class="welcome-badge">
        <span class="username"><i class="fas fa-user-circle me-2"></i><%= user.username %></span>
        <span class="role ms-3">(<%= user.role %>)</span>
      </div>

      <div class="text-end mb-4">
        <a href="/addProduct" class="btn btn-add">
          <i class="fas fa-plus-circle me-2"></i>Add New Vehicle
        </a>
      </div>

      <div class="inventory-card">
        <div class="table-responsive">
          <table class="inventory-table table table-hover">
            <thead>
              <tr>
                <th><i class="fas fa-car me-2"></i>Vehicle Name</th>
                <th><i class="fas fa-image me-2"></i>Image</th>
                <th><i class="fas fa-boxes me-2"></i>Stock</th>
                <th><i class="fas fa-dollar-sign me-2"></i>Price</th>
                <th><i class="fas fa-tools me-2"></i>Actions</th>
              </tr>
            </thead>
            <tbody>
              <% for(let i=0; i < products.length; i++) { %>
                <tr>
                  <td>
                    <a href="/product/<%= products[i].id %>">
                      <i class="fas fa-arrow-right me-2"></i><%= products[i].productName %>
                    </a>
                  </td>
                  <td>
                    <img src="images/<%= products[i].image %>" width="80" height="60" style="object-fit: cover;">
                  </td>
                  <td>
                    <span style="font-weight: 600; color: <%= products[i].quantity > 5 ? '#27ae60' : products[i].quantity > 0 ? '#f39c12' : '#e74c3c' %>">
                      <%= products[i].quantity %>
                    </span>
                  </td>
                  <td style="font-weight: 700; color: #ff3333; font-size: 1.1rem;">
                    $<%= products[i].price.toFixed(2) %>
                  </td>
                  <td>
                    <a href="/deleteProduct/<%= products[i].id %>" 
                       class="action-link delete">
                      <i class="fas fa-trash me-1"></i>Delete
                    </a>
                  </td>
                </tr>
              <% } %>
            </tbody>
          </table>
        </div>
      </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

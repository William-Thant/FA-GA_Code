<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Payment Successful - Isaac's Cardealership</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        body { 
            background: linear-gradient(135deg, #0a0a0a 0%, #1a1a1a 100%);
            min-height: 100vh;
            color: #ffffff;
        }
        .page-header { 
            background: linear-gradient(135deg, #000000 0%, #1a1a1a 50%, #00d4ff 100%);
            color: white; 
            padding: 60px 0; 
            margin-bottom: 40px;
            border-bottom: 3px solid #00d4ff;
            box-shadow: 0 10px 40px rgba(0, 212, 255, 0.3);
        }
        .page-header h1 {
            font-weight: 800;
            text-transform: uppercase;
            letter-spacing: 3px;
            text-shadow: 0 0 20px rgba(0, 212, 255, 0.5);
        }
        .confirmation-card { 
            background: linear-gradient(135deg, #1a1a1a 0%, #0a0a0a 100%);
            border: 2px solid #333;
            border-radius: 15px; 
            box-shadow: 0 10px 30px rgba(0,0,0,0.5); 
            padding: 40px; 
            text-align: center; 
        }
        .confirmation-card h2 {
            color: #00d4ff;
            font-weight: 800;
            text-transform: uppercase;
            letter-spacing: 2px;
            text-shadow: 0 0 15px rgba(0, 212, 255, 0.5);
        }
        .success-icon { 
            font-size: 4rem; 
            color: #00d4ff; 
            margin-bottom: 20px; 
            animation: scaleIn 0.5s ease-out;
            text-shadow: 0 0 30px rgba(0, 212, 255, 0.7);
        }
        .order-id { 
            font-size: 1.3rem; 
            font-weight: 800; 
            color: #ffd700; 
            margin: 20px 0;
            text-shadow: 0 0 15px rgba(255, 215, 0, 0.5);
        }
        .payment-badge {
            display: inline-block;
            background: linear-gradient(135deg, #ff3333 0%, #cc0000 100%);
            color: white;
            padding: 10px 25px;
            border-radius: 20px;
            font-size: 0.9rem;
            margin: 10px 0;
            font-weight: 800;
            text-transform: uppercase;
            letter-spacing: 1px;
            border: 2px solid #ff3333;
        }
        .text-muted {
            color: #a8a8a8 !important;
            font-weight: 600;
        }
        .alert-info {
            background: rgba(0, 212, 255, 0.1);
            border: 2px solid #00d4ff;
            color: #00d4ff;
            font-weight: 600;
        }
        .btn-success {
            background: linear-gradient(135deg, #ff3333 0%, #cc0000 100%);
            border: 2px solid #ff3333;
            color: white;
            font-weight: 800;
            text-transform: uppercase;
            letter-spacing: 2px;
        }
        .btn-success:hover {
            background: linear-gradient(135deg, #cc0000 0%, #990000 100%);
            transform: scale(1.02);
            box-shadow: 0 10px 30px rgba(255, 51, 51, 0.5);
        }
        .btn-outline-secondary {
            border: 2px solid #333;
            color: #ffffff;
            font-weight: 700;
            text-transform: uppercase;
        }
        .btn-outline-secondary:hover {
            background: linear-gradient(135deg, #333 0%, #1a1a1a 100%);
            border-color: #00d4ff;
            color: #ffffff;
        }
        .bg-light {
            background: rgba(0, 0, 0, 0.3) !important;
            border: 2px solid #333 !important;
        }
        .card-title {
            color: #00d4ff;
            font-weight: 800;
        }
        .card-text {
            color: #a8a8a8;
        }
        hr {
            border-color: #333;
            opacity: 1;
        }
        @keyframes scaleIn {
            0% { transform: scale(0); }
            50% { transform: scale(1.2); }
            100% { transform: scale(1); }
        }
    </style>
</head>
<body>
<%- include('partials/navbar') %>

<div class="page-header">
  <div class="container">
    <h1><i class="fas fa-check-circle"></i> Payment Confirmation</h1>
  </div>
</div>

<div class="container pb-5">
  <div class="row justify-content-center">
    <div class="col-lg-8">
      <div class="confirmation-card">
        <div class="success-icon">
          <i class="fas fa-check-circle"></i>
        </div>
        <h2 class="mb-3">Payment Successful!</h2>
        <% if (typeof paymentMethod !== 'undefined' && paymentMethod) { %>
            <div class="payment-badge"><%= paymentMethod %> Payment</div>
        <% } %>
        <p class="text-muted mb-4"><%= message %></p>

        <% if (typeof orderId !== 'undefined' && orderId) { %>
            <div class="order-id">
                Order ID: <%= orderId %>
            </div>
        <% } %>

        <hr class="my-4">

        <div class="alert alert-info mb-4">
          <i class="fas fa-envelope"></i> A receipt has been sent to your email address.
        </div>

        <div class="d-grid gap-2">
          <a href="/shop" class="btn btn-success btn-lg">
            <i class="fas fa-shopping-bag"></i> Continue Shopping
          </a>
          <a href="/" class="btn btn-outline-secondary">
            <i class="fas fa-home"></i> Back to Home
          </a>
        </div>
      </div>

      <div class="card mt-4 bg-light border-0">
        <div class="card-body">
          <h6 class="card-title"><i class="fas fa-truck"></i> Order Processing</h6>
          <p class="card-text small mb-0">Your order is being processed. You can track your order using the Order ID above.</p>
        </div>
      </div>
    </div>
  </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Order Confirmation - Isaac's Cardealership</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
  <style>
    body { 
      background: linear-gradient(135deg, #0a0a0a 0%, #1a1a1a 100%);
      min-height: 100vh;
      color: #ffffff;
    }
    .page-header { 
      background: linear-gradient(135deg, #00d4ff 0%, #0099cc 100%);
      color: white; 
      padding: 60px 0; 
      margin-bottom: 40px;
      border-bottom: 3px solid #00d4ff;
      box-shadow: 0 10px 40px rgba(0, 212, 255, 0.3);
    }
    .page-header h1 {
      font-weight: 800;
      text-transform: uppercase;
      letter-spacing: 3px;
      font-size: 2.5rem;
      text-shadow: 0 2px 10px rgba(0, 0, 0, 0.3);
    }
    .confirmation-card { 
      background: linear-gradient(135deg, #1a1a1a 0%, #0a0a0a 100%);
      border: 2px solid #00d4ff;
      border-radius: 15px;
      box-shadow: 0 10px 40px rgba(0, 212, 255, 0.3);
      padding: 50px 40px;
      text-align: center;
    }
    .success-icon { 
      font-size: 5rem;
      color: #00d4ff;
      margin-bottom: 30px;
      filter: drop-shadow(0 0 30px rgba(0, 212, 255, 0.6));
      animation: pulse 2s infinite;
    }
    @keyframes pulse {
      0%, 100% { transform: scale(1); }
      50% { transform: scale(1.05); }
    }
    .confirmation-card h2 {
      color: #ffffff;
      font-weight: 800;
      text-transform: uppercase;
      letter-spacing: 2px;
      margin-bottom: 15px;
    }
    .confirmation-card .text-muted {
      color: #a8a8a8 !important;
      font-size: 1.1rem;
    }
    .order-id { 
      font-size: 1.5rem;
      font-weight: 900;
      color: #ff3333;
      margin: 20px 0;
      text-transform: uppercase;
      letter-spacing: 2px;
      text-shadow: 0 0 10px rgba(255, 51, 51, 0.5);
    }
    .order-item { 
      border-bottom: 2px solid #2a2a2a;
      padding: 20px 0;
      text-align: left;
      transition: all 0.3s ease;
    }
    .order-item:last-child { 
      border-bottom: none;
    }
    .order-item:hover {
      background: rgba(0, 212, 255, 0.05);
      padding-left: 10px;
      padding-right: 10px;
    }
    .order-item h6 {
      color: #ffffff;
      font-weight: 700;
      font-size: 1.1rem;
    }
    .order-item small {
      color: #00d4ff !important;
      font-weight: 600;
    }
    .order-item .text-success {
      color: #ffd700 !important;
      font-weight: 800;
    }
    .order-summary { 
      background: rgba(0, 0, 0, 0.3);
      border: 2px solid #2a2a2a;
      padding: 25px;
      border-radius: 10px;
      margin: 30px 0;
      text-align: left;
    }
    .summary-row { 
      display: flex;
      justify-content: space-between;
      padding: 12px 0;
      color: #ffffff;
      font-size: 1.1rem;
    }
    .summary-row span:last-child {
      color: #ffd700;
      font-weight: 700;
    }
    .summary-row.total { 
      font-size: 1.5rem;
      font-weight: 900;
      color: #00d4ff;
      border-top: 3px solid #ff3333;
      padding-top: 20px;
      margin-top: 10px;
    }
    .summary-row.total span {
      color: #00d4ff !important;
      text-shadow: 0 0 15px rgba(0, 212, 255, 0.5);
    }
    hr {
      border-color: #2a2a2a;
      opacity: 1;
      border-width: 2px;
    }
    .confirmation-card h5 {
      color: #ff3333;
      font-weight: 800;
      text-transform: uppercase;
      letter-spacing: 2px;
    }
    .alert-info {
      background: rgba(0, 212, 255, 0.1);
      border: 2px solid #00d4ff;
      color: #00d4ff;
      border-radius: 10px;
      font-weight: 600;
    }
    .btn-primary {
      background: linear-gradient(135deg, #ff3333 0%, #cc0000 100%);
      border: none;
      color: white;
      padding: 15px;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 1px;
      transition: all 0.3s ease;
    }
    .btn-primary:hover {
      background: linear-gradient(135deg, #cc0000 0%, #990000 100%);
      transform: translateY(-2px);
      box-shadow: 0 5px 20px rgba(255, 51, 51, 0.5);
    }
    .btn-outline-info {
      border: 2px solid #00d4ff;
      color: #00d4ff;
      padding: 12px;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 1px;
      background: transparent;
    }
    .btn-outline-info:hover {
      background: #00d4ff;
      border-color: #00d4ff;
      color: #000000;
      transform: translateY(-2px);
      box-shadow: 0 5px 20px rgba(0, 212, 255, 0.5);
    }
    .btn-outline-secondary {
      border: 2px solid #2a2a2a;
      color: #888888;
      padding: 12px;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 1px;
      background: transparent;
    }
    .btn-outline-secondary:hover {
      background: #2a2a2a;
      border-color: #2a2a2a;
      color: #ffffff;
    }
    .card {
      background: linear-gradient(135deg, #1a1a1a 0%, #0f0f0f 100%);
      border: 2px solid #2a2a2a;
      color: #ffffff;
    }
    .card-title {
      color: #ff3333;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 1px;
    }
    .card-text {
      color: #a8a8a8 !important;
    }
  </style>
</head>
<body>
<%- include('../partials/navbar') %>

<div class="page-header">
  <div class="container">
    <h1><i class="fas fa-check-circle"></i> VEHICLE PURCHASE CONFIRMED</h1>
  </div>
</div>

<div class="container pb-5">
  <div class="row justify-content-center">
    <div class="col-lg-8">
      <div class="confirmation-card">
        <div class="success-icon">
          <i class="fas fa-check-circle"></i>
        </div>
        <h2 class="mb-3">Purchase Successful!</h2>
        <p class="text-muted mb-4">Thank you for your purchase. Your vehicle order has been confirmed.</p>

        <div class="order-id">
          <i class="fas fa-file-invoice"></i> Order #<%= order.orderId %>
        </div>
        
        <p class="text-muted small">
          📅 Order Date: <%= new Date(order.orderDate).toLocaleString() %>
        </p>

        <hr class="my-4">

        <h5 class="mb-3 text-start"><i class="fas fa-car"></i> Vehicle Details</h5>
        <% order.items.forEach(item => { %>
          <div class="order-item">
            <div class="row align-items-center">
              <div class="col-md-6">
                <h6 class="mb-1"><%= item.productName %></h6>
                <small class="text-muted"><%= item.quantity %> × $<%= item.price.toFixed(2) %></small>
              </div>
              <div class="col-md-6 text-end">
                <h6 class="mb-0 text-success">$<%= (item.price * item.quantity).toFixed(2) %></h6>
              </div>
            </div>
          </div>
        <% }) %>

        <div class="order-summary">
          <div class="summary-row">
            <span>Subtotal:</span>
            <span>$<%= order.subtotal.toFixed(2) %></span>
          </div>
          <div class="summary-row">
            <span>Tax (10%):</span>
            <span>$<%= order.tax.toFixed(2) %></span>
          </div>
          <div class="summary-row total">
            <span>Total Paid:</span>
            <span>$<%= order.total.toFixed(2) %></span>
          </div>
        </div>

        <hr class="my-4">

        <div class="alert alert-info mb-4">
          <i class="fas fa-envelope"></i> A confirmation email receipt has been sent to your registered email address.
        </div>

        <div class="d-grid gap-2">
          <a href="/shop" class="btn btn-primary btn-lg">
            <i class="fas fa-car-side"></i> Browse More Vehicles
          </a>
          <button id="resendReceiptBtn" class="btn btn-outline-info" type="button"><i class="fas fa-envelope"></i> Resend Receipt</button>
          <a href="/" class="btn btn-outline-secondary">
            <i class="fas fa-home"></i> Back to Home
          </a>
        </div>
      </div>

      <div class="card mt-4 border-0">
        <div class="card-body">
          <h6 class="card-title"><i class="fas fa-truck"></i> DELIVERY INFORMATION</h6>
          <p class="card-text small mb-0">Your vehicle will be prepared for delivery. We'll contact you shortly to arrange the delivery details and documentation.</p>
        </div>
      </div>
    </div>
  </div>
</div>

</body>
</html>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
<script>
document.addEventListener('DOMContentLoaded', function () {
  const btn = document.getElementById('resendReceiptBtn');
  if (!btn) return;
  btn.addEventListener('click', function () {
    btn.disabled = true;
    const originalText = btn.innerHTML;
    btn.innerHTML = '<i class="fas fa-spinner fa-spin"></i> Sending...';

    fetch('/resend-receipt', { method: 'POST', headers: { 'X-Requested-With': 'XMLHttpRequest' } })
      .then(r => r.json())
      .then(data => {
        if (data && data.success) {
          btn.innerHTML = '<i class="fas fa-check"></i> Sent';
          setTimeout(() => { btn.innerHTML = originalText; btn.disabled = false; }, 3000);
        } else {
          alert('Could not send receipt: ' + (data.message || 'Unknown error'));
          btn.innerHTML = originalText;
          btn.disabled = false;
        }
      }).catch(err => {
        console.error('Resend error', err);
        alert('Server error while sending receipt');
        btn.innerHTML = originalText;
        btn.disabled = false;
      });
  });
});
</script>

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Request Refund - Car Dealership</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        body {
            background: linear-gradient(135deg, #0a0a0a 0%, #1a1a1a 100%);
            min-height: 100vh;
            color: #ffffff !important;
        }
        .container {
            margin-top: 30px;
            margin-bottom: 50px;
        }
        .page-header {
            background: linear-gradient(135deg, #ff3333 0%, #cc0000 100%);
            padding: 30px;
            border-radius: 15px;
            margin-bottom: 30px;
            box-shadow: 0 10px 30px rgba(255, 51, 51, 0.3);
        }
        .page-header h1 {
            margin: 0;
            color: #ffffff !important;
        }
        .refund-form {
            background: linear-gradient(135deg, #1a1a1a 0%, #2a2a2a 100%);
            border: 2px solid #333;
            border-radius: 15px;
            padding: 30px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.5);
        }
        .order-summary {
            background: rgba(0, 0, 0, 0.3);
            border: 1px solid #444;
            border-radius: 10px;
            padding: 20px;
            margin-bottom: 30px;
        }
        .order-summary h5 {
            color: #ff3333 !important;
            margin-bottom: 15px;
        }
        .order-detail {
            display: flex;
            justify-content: space-between;
            padding: 8px 0;
            color: #ccc !important;
        }
        .order-detail strong {
            color: #ffffff !important;
        }
        .form-label {
            color: #ffffff !important;
            font-weight: bold;
            margin-bottom: 10px;
        }
        .form-control, .form-select {
            background: #0a0a0a !important;
            border: 2px solid #333 !important;
            color: #ffffff !important;
            padding: 12px;
            border-radius: 8px;
        }
        .form-control:focus, .form-select:focus {
            background: #1a1a1a !important;
            border-color: #ff3333 !important;
            color: #ffffff !important;
            box-shadow: 0 0 15px rgba(255, 51, 51, 0.3);
        }
        .form-select option {
            background: #1a1a1a;
            color: #ffffff;
        }
        textarea.form-control {
            min-height: 120px;
            resize: vertical;
        }
        .submit-btn {
            background: linear-gradient(135deg, #ff3333 0%, #cc0000 100%);
            border: none;
            color: #ffffff !important;
            padding: 15px 40px;
            border-radius: 25px;
            font-weight: bold;
            font-size: 1.1rem;
            transition: all 0.3s ease;
            width: 100%;
        }
        .submit-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 20px rgba(255, 51, 51, 0.4);
        }
        .cancel-btn {
            background: linear-gradient(135deg, #666 0%, #444 100%);
            border: none;
            color: #ffffff !important;
            padding: 15px 40px;
            border-radius: 25px;
            font-weight: bold;
            font-size: 1.1rem;
            transition: all 0.3s ease;
            width: 100%;
            margin-top: 15px;
        }
        .cancel-btn:hover {
            background: linear-gradient(135deg, #777 0%, #555 100%);
        }
        .item-select {
            background: rgba(0, 0, 0, 0.3);
            border: 2px solid #333;
            padding: 15px;
            border-radius: 10px;
            margin-bottom: 10px;
            transition: all 0.3s ease;
        }
        .item-select:hover {
            border-color: #ff3333;
        }
        .item-select input[type="radio"] {
            width: 20px;
            height: 20px;
            margin-right: 10px;
        }
        .item-info {
            color: #ccc !important;
        }
        .item-price {
            color: #ff3333 !important;
            font-weight: bold;
            font-size: 1.2rem;
        }
    </style>
</head>
<body>
    <%- include('../partials/navbar') %>
    
    <div class="container">
        <div class="page-header">
            <h1><i class="fas fa-undo"></i> Request Refund</h1>
        </div>
        
        <% if (messages.error && messages.error.length > 0) { %>
            <div class="alert alert-danger">
                <%= messages.error[0] %>
            </div>
        <% } %>
        
        <div class="refund-form">
            <div class="order-summary">
                <h5><i class="fas fa-receipt"></i> Order Summary</h5>
                <div class="order-detail">
                    <span>Order ID:</span>
                    <strong>#<%= order.orderId %></strong>
                </div>
                <div class="order-detail">
                    <span>Order Date:</span>
                    <strong><%= order.orderDate ? new Date(order.orderDate).toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' }) : 'N/A' %></strong>
                </div>
                <div class="order-detail">
                    <span>Order Total:</span>
                    <strong>$<%= parseFloat(order.total || 0).toFixed(2) %></strong>
                </div>
            </div>
            
            <form action="/orders/<%= order.orderId %>/refund" method="POST">
                <div class="mb-4">
                    <label class="form-label">
                        <i class="fas fa-car"></i> Select Item to Refund
                    </label>
                    <% items.forEach(item => { %>
                        <label class="item-select d-flex align-items-center">
                            <input type="radio" name="productId" value="<%= item.productId %>" required>
                            <div class="flex-grow-1">
                                <div class="item-info">
                                    <strong style="color: #ffffff !important;"><%= item.productName %></strong>
                                </div>
                                <div class="item-info">Quantity: <%= item.quantity %></div>
                            </div>
                            <div class="item-price">$<%= (parseFloat(item.price || 0) * parseFloat(item.quantity || 0)).toFixed(2) %></div>
                        </label>
                    <% }); %>
                </div>
                
                <div class="mb-4">
                    <label for="amount" class="form-label">
                        <i class="fas fa-dollar-sign"></i> Full Refund Amount
                    </label>
                    <input type="number" step="0.01" class="form-control" id="amount" name="amount" 
                           placeholder="Select an item to see refund amount" readonly>
                    <small style="color: #999;">Full refund for the selected item (read-only)</small>
                </div>
                
                <div class="mb-4">
                    <label for="reason" class="form-label">
                        <i class="fas fa-comment-alt"></i> Reason for Refund
                    </label>
                    <textarea class="form-control" id="reason" name="reason" 
                              placeholder="Please explain why you are requesting a refund..." required></textarea>
                    <small style="color: #999;">Provide a detailed explanation for your refund request</small>
                </div>
                
                <button type="submit" class="btn submit-btn">
                    <i class="fas fa-paper-plane"></i> Submit Refund Request
                </button>
                
                <a href="/orders" class="btn cancel-btn">
                    <i class="fas fa-times"></i> Cancel
                </a>
            </form>
        </div>
    </div>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // Auto-fill the full refund amount when an item is selected
        document.querySelectorAll('input[name="productId"]').forEach(radio => {
            radio.addEventListener('change', function() {
                const priceText = this.closest('.item-select').querySelector('.item-price').textContent;
                const price = parseFloat(priceText.replace('$', ''));
                document.getElementById('amount').value = price.toFixed(2);
            });
        });
        
        // Ensure at least one item is selected on form submission
        document.querySelector('form').addEventListener('submit', function(e) {
            const selectedProduct = document.querySelector('input[name="productId"]:checked');
            if (!selectedProduct) {
                e.preventDefault();
                alert('Please select an item to request a refund for');
            }
        });
    </script>
</body>
</html>

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <style>
    body { 
      background: linear-gradient(135deg, #0a0a0a 0%, #1a1a1a 100%);
      min-height: 100vh;
      color: #ffffff;
    }
    .page-header { 
      background: linear-gradient(135deg, #ffd700 0%, #000000 50%, #ffd700 100%);
      color: white; 
      padding: 50px 0; 
      margin-bottom: 40px;
      border-bottom: 4px solid #ffd700;
      box-shadow: 0 15px 50px rgba(255, 215, 0, 0.4);
    }
    .page-header h1 {
      font-weight: 900;
      text-transform: uppercase;
      letter-spacing: 4px;
      text-shadow: 0 0 30px rgba(255, 51, 51, 0.8);
    }
    .page-header p {
      color: #ffffff;
      letter-spacing: 2px;
      font-weight: 600;
    }
    .form-card { 
      background: linear-gradient(135deg, #1a1a1a 0%, #0a0a0a 100%);
      border: 2px solid #ffd700;
      border-radius: 15px;
      box-shadow: 0 10px 30px rgba(255, 215, 0, 0.3);
      padding: 40px;
    }
    .form-label {
      color: #ffd700;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 1px;
      font-size: 0.9rem;
    }
    .form-control, .form-select {
      background: #0a0a0a;
      border: 2px solid #333;
      color: #ffffff;
      border-radius: 10px;
      padding: 12px;
      font-weight: 600;
    }
    .form-control:focus, .form-select:focus {
      background: #0a0a0a;
      border-color: #ffd700;
      color: #ffffff;
      box-shadow: 0 0 15px rgba(255, 215, 0, 0.3);
    }
    .form-control::placeholder {
      color: #ffffff;
      opacity: 0.5;
    }
    .input-group-text {
      background: #ffd700;
      border: 2px solid #ffd700;
      color: #0a0a0a;
      font-weight: 700;
    }
    .text-muted {
      color: #ffffff !important;
      opacity: 0.6;
      font-size: 0.85rem;
    }
    .btn-racing {
      background: linear-gradient(135deg, #ff3333 0%, #cc0000 100%);
      border: none;
      color: white;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 1px;
      padding: 15px 40px;
      border-radius: 10px;
      transition: all 0.3s;
    }
    .btn-racing:hover {
      background: linear-gradient(135deg, #cc0000 0%, #ff3333 100%);
      transform: scale(1.05);
      box-shadow: 0 8px 25px rgba(255, 51, 51, 0.5);
      color: white;
    }
    .btn-back {
      background: linear-gradient(135deg, #333 0%, #1a1a1a 100%);
      border: 2px solid #ffd700;
      color: #ffd700;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 1px;
      padding: 15px 40px;
      border-radius: 10px;
      transition: all 0.3s;
    }
    .btn-back:hover {
      background: linear-gradient(135deg, #ffd700 0%, #ccaa00 100%);
      border-color: #ffd700;
      color: #0a0a0a;
      box-shadow: 0 8px 25px rgba(255, 215, 0, 0.5);
    }
    input[type="file"]::-webkit-file-upload-button {
      background: linear-gradient(135deg, #ffd700 0%, #ccaa00 100%);
      border: none;
      color: #0a0a0a;
      font-weight: 700;
      padding: 10px 20px;
      border-radius: 8px;
      cursor: pointer;
      text-transform: uppercase;
      letter-spacing: 1px;
    }
    input[type="file"]::-webkit-file-upload-button:hover {
      background: linear-gradient(135deg, #ccaa00 0%, #ffd700 100%);
    }
  </style>
  <title>Add Vehicle - Isaac's Cardealership</title>
</head>
<body>
<%- include('../partials/navbar') %>

<div class="page-header">
  <div class="container">
    <h1><i class="fas fa-plus-circle"></i> 🏁 ADD NEW VEHICLE</h1>
    <p class="mb-0">Add a new vehicle to your dealership showroom</p>
  </div>
</div>

<div class="container pb-5">
  <div class="row justify-content-center">
    <div class="col-lg-8">
      <div class="form-card">
        <form action="/addProduct" method="POST" enctype="multipart/form-data">

          <div class="mb-3">
            <label for="name" class="form-label"><i class="fas fa-car me-2"></i>Vehicle Name</label>
            <input type="text" class="form-control" id="name" name="name" placeholder="e.g., 2024 Ferrari F8 Tributo" required>
          </div>

          <div class="row">
            <div class="col-md-6 mb-3">
              <label for="quantity" class="form-label"><i class="fas fa-warehouse me-2"></i>Stock Quantity</label>
              <input type="number" class="form-control" id="quantity" name="quantity" min="0" step="1" placeholder="0" required>
            </div>

            <div class="col-md-6 mb-3">
              <label for="price" class="form-label"><i class="fas fa-dollar-sign me-2"></i>Price</label>
              <div class="input-group">
                <span class="input-group-text">$</span>
                <input type="number" class="form-control" id="price" name="price" min="0" step="0.01" placeholder="0.00" required>
              </div>
            </div>
          </div>

          <div class="mb-3">
            <label for="category" class="form-label"><i class="fas fa-tag me-2"></i>Category</label>
            <select class="form-select" id="category" name="category">
              <option value="">Select a category (optional)</option>
              <option value="Sports Cars">Sports Cars</option>
              <option value="Luxury Sedans">Luxury Sedans</option>
              <option value="SUVs">SUVs</option>
              <option value="Electric Vehicles">Electric Vehicles</option>
              <option value="Classic Cars">Classic Cars</option>
              <option value="Supercars">Supercars</option>
            </select>
          </div>

          <div class="mb-4">
            <label for="image" class="form-label"><i class="fas fa-image me-2"></i>Vehicle Image</label>
            <input class="form-control" type="file" id="image" name="image" accept="image/*" required>
            <small class="text-muted">Recommended: High-quality images, minimum 800x600px</small>
          </div>

          <div class="d-grid gap-3">
            <button type="submit" class="btn btn-racing btn-lg"><i class="fas fa-check-circle"></i> Add Vehicle to Showroom</button>
            <a href="/inventory" class="btn btn-back"><i class="fas fa-arrow-left"></i> Back to Inventory</a>
          </div>

        </form>
      </div>
    </div>
  </div>
</div>
</body>
</html>

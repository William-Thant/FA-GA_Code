<!DOCTYPE html>
<html lang="en" data-theme="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link rel="stylesheet" href="/theme.css">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <title><%= product.productName %> - Isaac's Cardealership</title>
    <style>
        html, body {
            background: #0a0a0a !important;
            color: #ffffff !important;
        }

        body {
            background: linear-gradient(135deg, #0a0a0a 0%, #0a0a0a 100%) !important;
        }

        .page-header {
            background: linear-gradient(135deg, rgba(255, 51, 51, 0.1) 0%, rgba(0, 0, 0, 0.9) 100%);
            padding: 60px 0 80px;
            margin-bottom: 40px;
            border-bottom: 3px solid #ff3333;
        }

        .page-header h1 {
            font-size: 3rem;
            font-weight: 900;
            color: #ffffff;
            text-transform: uppercase;
            letter-spacing: 4px;
            text-shadow: 0 0 40px rgba(255, 51, 51, 0.5);
            margin-bottom: 10px;
        }

        .breadcrumb-link {
            color: #00d4ff;
            text-decoration: none;
            font-size: 1.1rem;
        }

        .breadcrumb-link:hover {
            color: #ff3333;
            text-decoration: underline;
        }

        .product-gallery {
            position: relative;
        }

        .main-image {
            width: 100%;
            height: 500px;
            object-fit: cover;
            border-radius: 15px;
            border: 2px solid #333;
            background: #1a1a1a;
            margin-bottom: 20px;
        }

        .details-card {
            background: linear-gradient(145deg, #141414, #1a1a1a);
            padding: 40px;
            border-radius: 15px;
            border: 1px solid #333;
            margin-bottom: 30px;
        }

        .product-title {
            font-size: 2.5rem;
            font-weight: 900;
            color: #ffffff;
            margin-bottom: 15px;
            letter-spacing: 1px;
        }

        .product-meta {
            display: flex;
            gap: 20px;
            margin-bottom: 30px;
            flex-wrap: wrap;
        }

        .meta-item {
            display: flex;
            align-items: center;
            gap: 8px;
            background: rgba(255, 51, 51, 0.1);
            padding: 10px 15px;
            border-radius: 8px;
            border-left: 3px solid #ff3333;
        }

        .meta-item i {
            color: #ff3333;
            font-size: 1.2rem;
        }

        .meta-item span {
            color: #a8a8a8;
            font-size: 0.95rem;
        }

        .price-section {
            background: linear-gradient(145deg, #1a1a1a, #141414);
            padding: 30px;
            border-radius: 15px;
            border: 2px solid #ff3333;
            margin-bottom: 30px;
            text-align: center;
        }

        .price-label {
            color: #a8a8a8;
            font-size: 1rem;
            margin-bottom: 10px;
        }

        .price-value {
            font-size: 3rem;
            font-weight: 900;
            color: #ff3333;
            letter-spacing: 2px;
        }

        .specs-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
            margin-bottom: 30px;
        }

        .spec-card {
            background: linear-gradient(145deg, #141414, #1a1a1a);
            padding: 25px;
            border-radius: 12px;
            border: 1px solid #333;
            text-align: center;
            transition: all 0.3s ease;
        }

        .spec-card:hover {
            border-color: #ff3333;
            transform: translateY(-5px);
            box-shadow: 0 10px 30px rgba(255, 51, 51, 0.2);
        }

        .spec-icon {
            font-size: 2rem;
            color: #ff3333;
            margin-bottom: 10px;
        }

        .spec-label {
            color: #a8a8a8;
            font-size: 0.9rem;
            margin-bottom: 8px;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .spec-value {
            font-size: 1.5rem;
            font-weight: 700;
            color: #ffffff;
        }

        .seller-card {
            background: linear-gradient(145deg, rgba(255, 51, 51, 0.1), #141414);
            padding: 30px;
            border-radius: 15px;
            border: 2px solid rgba(255, 51, 51, 0.3);
            margin-bottom: 30px;
        }

        .seller-title {
            color: #ff3333;
            font-size: 1.3rem;
            font-weight: 700;
            margin-bottom: 20px;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .seller-info {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
        }

        .seller-item {
            background: #0a0a0a;
            padding: 15px;
            border-radius: 10px;
            border-left: 3px solid #ff3333;
        }

        .seller-item-label {
            color: #a8a8a8;
            font-size: 0.85rem;
            text-transform: uppercase;
            letter-spacing: 1px;
            margin-bottom: 5px;
        }

        .seller-item-value {
            color: #ffffff;
            font-size: 1.1rem;
            font-weight: 700;
        }

        .description-section {
            background: linear-gradient(145deg, #141414, #1a1a1a);
            padding: 30px;
            border-radius: 15px;
            border: 1px solid #333;
            margin-bottom: 30px;
        }

        .section-title {
            color: #ffffff;
            font-size: 1.5rem;
            font-weight: 700;
            margin-bottom: 20px;
            display: flex;
            align-items: center;
            gap: 10px;
            padding-bottom: 15px;
            border-bottom: 2px solid #ff3333;
        }

        .section-title i {
            color: #ff3333;
        }

        .description-text {
            color: #a8a8a8;
            line-height: 1.8;
            font-size: 1.05rem;
        }

        .action-buttons {
            display: flex;
            gap: 15px;
            margin-bottom: 30px;
            flex-wrap: wrap;
        }

        .btn-primary-custom {
            background: linear-gradient(145deg, #ff3333, #ff1a1a);
            color: #ffffff;
            border: none;
            padding: 15px 40px;
            font-size: 1.1rem;
            font-weight: 700;
            border-radius: 10px;
            cursor: pointer;
            transition: all 0.3s ease;
            text-transform: uppercase;
            letter-spacing: 2px;
            flex: 1;
            min-width: 200px;
        }

        .btn-primary-custom:hover {
            background: linear-gradient(145deg, #ff1a1a, #ff0000);
            transform: translateY(-3px);
            box-shadow: 0 10px 30px rgba(255, 51, 51, 0.4);
            color: #ffffff;
        }

        .btn-secondary-custom {
            background: linear-gradient(145deg, #00d4ff, #00a3cc);
            color: #ffffff;
            border: none;
            padding: 15px 40px;
            font-size: 1.1rem;
            font-weight: 700;
            border-radius: 10px;
            cursor: pointer;
            transition: all 0.3s ease;
            text-transform: uppercase;
            letter-spacing: 2px;
            flex: 1;
            min-width: 200px;
        }

        .btn-secondary-custom:hover {
            background: linear-gradient(145deg, #00a3cc, #007a99);
            transform: translateY(-3px);
            box-shadow: 0 10px 30px rgba(0, 212, 255, 0.4);
            color: #ffffff;
        }

        .back-link {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            color: #00d4ff;
            text-decoration: none;
            font-size: 1.1rem;
            margin-bottom: 30px;
            transition: all 0.3s ease;
        }

        .back-link:hover {
            color: #ff3333;
            gap: 12px;
        }

        .stock-badge {
            display: inline-block;
            padding: 10px 20px;
            border-radius: 25px;
            font-weight: 700;
            margin-bottom: 20px;
            text-transform: uppercase;
            letter-spacing: 1px;
            font-size: 0.9rem;
        }

        .stock-badge.in-stock {
            background: rgba(0, 212, 255, 0.2);
            color: #00d4ff;
            border: 1px solid #00d4ff;
        }

        .stock-badge.out-of-stock {
            background: rgba(255, 51, 51, 0.2);
            color: #ff3333;
            border: 1px solid #ff3333;
        }

        @media (max-width: 768px) {
            .product-title {
                font-size: 1.8rem;
            }

            .price-value {
                font-size: 2rem;
            }

            .main-image {
                height: 300px;
            }

            .action-buttons {
                flex-direction: column;
            }

            .btn-primary-custom, .btn-secondary-custom {
                min-width: 100%;
                flex: none;
            }
        }
    </style>
</head>
<body>
<%- include('../partials/navbar') %>

<div class="page-header">
    <div class="container">
        <a href="/shopping" class="breadcrumb-link"><i class="fas fa-chevron-left"></i> Back to Inventory</a>
        <h1><i class="fas fa-car"></i> Vehicle Details</h1>
    </div>
</div>

<div class="container">
    <% if (product) { %>
        <div class="row">
            <!-- Left Column: Image Gallery -->
            <div class="col-lg-6">
                <div class="product-gallery">
                    <% 
                        // Use uploaded image if available, otherwise use default
                        let imageUrl = 'https://images.unsplash.com/photo-1492144534655-ae79c964c9d7?w=800&h=600&fit=crop';
                        if (product.image) {
                            imageUrl = '/images/' + product.image;
                        }
                    %>
                    <img src="<%= imageUrl %>" alt="<%= product.productName %>" class="main-image">
                    <% if (product.condition_status) { %>
                        <% if (product.condition_status === 'new') { %>
                            <div class="stock-badge in-stock" style="position: absolute; top: 20px; left: 20px;">
                                <i class="fas fa-star"></i> Brand New
                            </div>
                        <% } else { %>
                            <div class="stock-badge in-stock" style="position: absolute; top: 20px; left: 20px;">
                                <i class="fas fa-history"></i> Pre-Owned
                            </div>
                        <% } %>
                    <% } %>
                </div>
            </div>

            <!-- Right Column: Product Details -->
            <div class="col-lg-6">
                <!-- Title & Meta -->
                <h1 class="product-title"><%= product.productName %></h1>
                
                <% if (product.year || product.mileage !== undefined || product.fuel_type) { %>
                    <div class="product-meta">
                        <% if (product.year) { %>
                            <div class="meta-item">
                                <i class="fas fa-calendar-alt"></i>
                                <span><strong><%= product.year %></strong> Model Year</span>
                            </div>
                        <% } %>
                        <% if (product.mileage !== undefined) { %>
                            <div class="meta-item">
                                <i class="fas fa-tachometer-alt"></i>
                                <span><strong><%= product.mileage.toLocaleString() %></strong> miles</span>
                            </div>
                        <% } %>
                        <% if (product.fuel_type) { %>
                            <div class="meta-item">
                                <i class="fas fa-gas-pump"></i>
                                <span><strong><%= product.fuel_type %></strong> Engine</span>
                            </div>
                        <% } %>
                    </div>
                <% } %>

                <!-- Stock Status -->
                <% if (product.quantity > 0) { %>
                    <div class="stock-badge in-stock">
                        <i class="fas fa-check-circle"></i> In Stock - Ready for Purchase
                    </div>
                <% } else { %>
                    <div class="stock-badge out-of-stock">
                        <i class="fas fa-times-circle"></i> Out of Stock
                    </div>
                <% } %>

                <!-- Price Section -->
                <div class="price-section">
                    <div class="price-label">PRICE</div>
                    <div class="price-value">$<%= product.price.toLocaleString() %></div>
                </div>

                <!-- Specifications Grid -->
                <div class="specs-grid">
                    <% if (product.category) { %>
                        <div class="spec-card">
                            <div class="spec-icon"><i class="fas fa-th"></i></div>
                            <div class="spec-label">Category</div>
                            <div class="spec-value"><%= product.category %></div>
                        </div>
                    <% } %>
                    <% if (product.transmission) { %>
                        <div class="spec-card">
                            <div class="spec-icon"><i class="fas fa-cog"></i></div>
                            <div class="spec-label">Transmission</div>
                            <div class="spec-value"><%= product.transmission %></div>
                        </div>
                    <% } %>
                    <% if (product.engine_cc) { %>
                        <div class="spec-card">
                            <div class="spec-icon"><i class="fas fa-bolt"></i></div>
                            <div class="spec-label">Engine</div>
                            <div class="spec-value"><%= product.engine_cc %> CC</div>
                        </div>
                    <% } %>
                    <% if (product.color) { %>
                        <div class="spec-card">
                            <div class="spec-icon"><i class="fas fa-palette"></i></div>
                            <div class="spec-label">Color</div>
                            <div class="spec-value"><%= product.color %></div>
                        </div>
                    <% } %>
                </div>

                <!-- Action Buttons -->
                <div class="action-buttons">
                    <% if (product.quantity > 0) { %>
                        <form action="/add-to-cart/<%= product.id %>" method="POST" style="flex: 1;">
                            <button type="submit" class="btn-primary-custom" style="width: 100%;">
                                <i class="fas fa-heart"></i> Add to Wishlist
                            </button>
                        </form>
                    <% } else { %>
                        <button class="btn-primary-custom" disabled style="opacity: 0.5; cursor: not-allowed; width: 100%;">
                            <i class="fas fa-times"></i> Sold Out
                        </button>
                    <% } %>
                    <a href="/shopping" class="btn-secondary-custom" style="text-decoration: none; display: flex; align-items: center; justify-content: center; color: #ffffff !important;">
                        <i class="fas fa-arrow-left"></i> Continue Shopping
                    </a>
                </div>
            </div>
        </div>

        <!-- Seller Information Card -->
        <% if (product.seller_id || product.username) { %>
            <div class="seller-card">
                <div class="seller-title">
                    <i class="fas fa-user-circle"></i> Seller Information
                </div>
                <div class="seller-info">
                    <% if (product.username) { %>
                        <div class="seller-item">
                            <div class="seller-item-label">Seller Name</div>
                            <div class="seller-item-value"><%= product.username %></div>
                        </div>
                    <% } %>
                    <% if (product.seller_id) { %>
                        <div class="seller-item">
                            <div class="seller-item-label">Seller ID</div>
                            <div class="seller-item-value">#<%= product.seller_id %></div>
                        </div>
                    <% } %>
                    <div class="seller-item">
                        <div class="seller-item-label">Seller Status</div>
                        <div class="seller-item-value">
                            <i class="fas fa-check-circle" style="color: #00d4ff;"></i> Verified Professional Dealer
                        </div>
                    </div>
                </div>
            </div>
        <% } %>

        <!-- Description Section -->
        <% if (product.description) { %>
            <div class="description-section">
                <div class="section-title">
                    <i class="fas fa-info-circle"></i> About This Vehicle
                </div>
                <div class="description-text">
                    <%= product.description %>
                </div>
            </div>
        <% } %>

        <!-- Additional Details -->
        <div class="description-section">
            <div class="section-title">
                <i class="fas fa-list-check"></i> Product Details
            </div>
            <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); gap: 20px;">
                <div>
                    <div style="color: #a8a8a8; margin-bottom: 8px; text-transform: uppercase; letter-spacing: 1px; font-size: 0.85rem;">Product ID</div>
                    <div style="color: #ffffff; font-weight: 700; font-size: 1.1rem;"><%= product.id %></div>
                </div>
                <div>
                    <div style="color: #a8a8a8; margin-bottom: 8px; text-transform: uppercase; letter-spacing: 1px; font-size: 0.85rem;">Quantity Available</div>
                    <div style="color: #ffffff; font-weight: 700; font-size: 1.1rem;"><%= product.quantity %> unit(s)</div>
                </div>
                <% if (product.year) { %>
                    <div>
                        <div style="color: #a8a8a8; margin-bottom: 8px; text-transform: uppercase; letter-spacing: 1px; font-size: 0.85rem;">Year</div>
                        <div style="color: #ffffff; font-weight: 700; font-size: 1.1rem;"><%= product.year %></div>
                    </div>
                <% } %>
            </div>
        </div>

    <% } else { %>
        <div class="text-center" style="padding: 80px 20px;">
            <i class="fas fa-car-slash" style="font-size: 5rem; color: #333; margin-bottom: 30px;"></i>
            <h2 style="color: #ffffff; font-size: 2rem; margin-bottom: 15px;">Vehicle Not Found</h2>
            <p style="color: #a8a8a8; font-size: 1.1rem; margin-bottom: 30px;">Sorry, this vehicle is not available in our inventory.</p>
            <a href="/shopping" class="btn-secondary-custom" style="text-decoration: none; display: inline-flex; align-items: center; gap: 10px;">
                <i class="fas fa-arrow-left"></i> Back to Inventory
            </a>
        </div>
    <% } %>
</div>

</body>
</html>

<!DOCTYPE html>
<html>
<head>
  <title>Wallet History - Isaac's Cardealership</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <link rel="stylesheet" href="/theme.css">
  <style>
    body { 
      background: linear-gradient(135deg, #0a0a0a 0%, #1a1a1a 100%);
      min-height: 100vh;
      color: #ffffff;
    }
    .page-header { 
      background: linear-gradient(135deg, #000000 0%, #1a1a1a 50%, #ff3333 100%);
      color: white; 
      padding: 60px 0; 
      margin-bottom: 40px;
      border-bottom: 3px solid #ff3333;
      box-shadow: 0 10px 40px rgba(255, 51, 51, 0.3);
    }
    .page-header h2 {
      font-weight: 800;
      text-transform: uppercase;
      letter-spacing: 3px;
      font-size: 2.5rem;
      text-shadow: 0 0 20px rgba(255, 51, 51, 0.5);
      margin: 0;
    }
    .wallet-card {
      background: linear-gradient(135deg, #1a1a1a 0%, #0a0a0a 100%);
      border: 2px solid #333;
      border-radius: 15px;
      box-shadow: 0 10px 30px rgba(0,0,0,0.5);
      padding: 30px;
      margin-bottom: 30px;
      transition: all 0.3s ease;
    }
    .wallet-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 15px 50px rgba(255, 51, 51, 0.3);
      border-color: #ff3333;
    }
    .transaction-item {
      background: rgba(0, 0, 0, 0.3);
      border: 1px solid #333;
      border-radius: 10px;
      padding: 20px;
      margin-bottom: 15px;
      transition: all 0.3s ease;
    }
    .transaction-item:hover {
      background: rgba(255, 51, 51, 0.1);
      border-color: #ff3333;
      transform: translateX(10px);
    }
    .amount-positive { 
      color: #00d4ff; 
      font-weight: 700;
      font-size: 1.2rem;
      text-shadow: 0 0 10px rgba(0, 212, 255, 0.3);
    }
    .amount-negative { 
      color: #ff3333; 
      font-weight: 700;
      font-size: 1.2rem;
      text-shadow: 0 0 10px rgba(255, 51, 51, 0.3);
    }
    .btn-topup {
      background: linear-gradient(135deg, #ff3333 0%, #cc0000 100%);
      border: 2px solid #ff3333;
      color: white !important;
      padding: 12px 30px;
      border-radius: 25px;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 1px;
      transition: all 0.3s ease;
      text-decoration: none;
    }
    .btn-topup:hover {
      background: linear-gradient(135deg, #cc0000 0%, #990000 100%);
      transform: scale(1.05);
      color: white !important;
      text-decoration: none;
      box-shadow: 0 5px 20px rgba(255, 51, 51, 0.5);
    }
    .empty-state {
      background: linear-gradient(135deg, #1a1a1a 0%, #0a0a0a 100%);
      border: 2px solid #333;
      border-radius: 15px;
      padding: 80px 40px;
      text-align: center;
    }
    .empty-state i {
      font-size: 6rem;
      color: #ff3333;
      margin-bottom: 30px;
      filter: drop-shadow(0 0 20px rgba(255, 51, 51, 0.5));
    }
    .empty-state p {
      color: #a8a8a8;
      font-size: 1.2rem;
    }
    .card {
      background: linear-gradient(135deg, #1a1a1a 0%, #0a0a0a 100%);
      border: 2px solid #333;
      border-radius: 15px;
      box-shadow: 0 10px 30px rgba(0,0,0,0.5);
    }
    .card-body {
      color: #ffffff;
    }
    .card-header {
      background: linear-gradient(135deg, #ff3333 0%, #cc0000 100%);
      border-bottom: 2px solid #ff3333;
      color: white;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 2px;
      border-radius: 13px 13px 0 0 !important;
    }
    .card-header h5 {
      margin: 0;
      font-size: 1.2rem;
    }
    .text-success {
      color: #ffd700 !important;
      font-weight: 800 !important;
      font-size: 2.5rem !important;
      text-shadow: 0 0 20px rgba(255, 215, 0, 0.5) !important;
    }
    .text-muted {
      color: #a8a8a8 !important;
    }
    .badge {
      padding: 8px 15px;
      font-size: 0.85rem;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 1px;
    }
    .bg-success {
      background: linear-gradient(135deg, #00d4ff 0%, #0099cc 100%) !important;
    }
    .bg-primary {
      background: linear-gradient(135deg, #ff3333 0%, #cc0000 100%) !important;
    }
    .bg-info {
      background: linear-gradient(135deg, #ffd700 0%, #cc9900 100%) !important;
    }
    .bg-warning {
      background: linear-gradient(135deg, #00d4ff 0%, #0099cc 100%) !important;
    }
    .bg-danger {
      background: linear-gradient(135deg, #ff3333 0%, #990000 100%) !important;
    }
    .list-group-item {
      background: transparent;
      border: none;
      padding: 0;
    }
    .list-group-flush {
      padding: 20px;
    }
  </style>
</head>
<body>
  <%- include('../partials/navbar') %>
  
  <div class="container my-5">
    <div class="row">
      <div class="col-md-8 offset-md-2">
        <div class="d-flex justify-content-between align-items-center mb-4">
          <h2><i class="fas fa-wallet"></i> WALLET HISTORY</h2>
          <a href="/wallet/topup" class="btn btn-topup">
            <i class="fas fa-plus-circle"></i> Top Up Wallet
          </a>
        </div>

        <!-- Current Balance Card -->
        <div class="card mb-4 text-center">
          <div class="card-body">
            <h5 class="text-muted mb-3" style="font-weight: 600; text-transform: uppercase; letter-spacing: 2px;">Current Balance</h5>
            <h2 class="text-success mb-0">$<%= walletBalance.toFixed(2) %></h2>
          </div>
        </div>

        <!-- Transactions List -->
        <div class="card">
          <div class="card-header">
            <h5 class="mb-0"><i class="fas fa-history"></i> Transaction History</h5>
          </div>
          <div class="card-body p-0">
            <% if (transactions.length === 0) { %>
              <div class="empty-state text-center py-5">
                <i class="fas fa-wallet"></i>
                <p class="text-muted mb-3">No transactions yet</p>
                <a href="/wallet/topup" class="btn btn-topup">Top Up Now</a>
              </div>
            <% } else { %>
              <div class="list-group list-group-flush">
                <% transactions.forEach(txn => { %>
                  <div class="list-group-item transaction-item">
                    <div class="d-flex justify-content-between align-items-start">
                      <div>
                        <div class="d-flex align-items-center mb-2">
                          <% if (txn.type === 'deposit') { %>
                            <span class="badge bg-success me-2"><i class="fas fa-arrow-down"></i> Deposit</span>
                          <% } else if (txn.type === 'purchase') { %>
                            <span class="badge bg-primary me-2"><i class="fas fa-car"></i> Purchase</span>
                          <% } else if (txn.type === 'refund') { %>
                            <span class="badge bg-info me-2"><i class="fas fa-undo"></i> Refund</span>
                          <% } else if (txn.type === 'admin_credit') { %>
                            <span class="badge bg-warning me-2"><i class="fas fa-gift"></i> Credit</span>
                          <% } else if (txn.type === 'admin_debit') { %>
                            <span class="badge bg-danger me-2"><i class="fas fa-minus-circle"></i> Debit</span>
                          <% } %>
                          <strong style="color: #ffffff; font-size: 1.1rem;"><%= txn.description || 'Wallet transaction' %></strong>
                        </div>
                        <small class="text-muted" style="font-size: 0.9rem;">
                          <i class="far fa-clock"></i> <%= new Date(txn.created_at).toLocaleString() %>
                          <% if (txn.reference) { %>
                            | <i class="fas fa-hashtag"></i> <%= txn.reference %>
                          <% } %>
                        </small>
                      </div>
                      <div class="text-end">
                        <% if (txn.type === 'deposit' || txn.type === 'refund' || txn.type === 'admin_credit') { %>
                          <div class="amount-positive">+$<%= txn.amount.toFixed(2) %></div>
                        <% } else { %>
                          <div class="amount-negative">-$<%= txn.amount.toFixed(2) %></div>
                        <% } %>
                        <small class="text-muted">Balance: <span style="color: #ffd700; font-weight: 600;">$<%= txn.balance_after.toFixed(2) %></span></small>
                      </div>
                    </div>
                  </div>
                <% }); %>
              </div>
            <% } %>
          </div>
        </div>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
